---
phase: 260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
  - tests/test_ci_workflow.py
  - tests/test_deploy_workflow.py
  - README.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
  - CI-04
must_haves:
  truths:
    - "ci.yml runs lint and test only on push to main and PRs targeting main"
    - "deploy.yml runs Docker build and GHCR push on main push only"
    - "PRs never trigger deploy.yml and never build or push Docker images"
    - "Main push runs ci.yml and deploy.yml in parallel with independent concurrency groups"
    - "Contract tests assert two-workflow layout and cover D-01 through D-14 across both files"
    - "README documents two workflows and PR vs main behavior"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: Lint+test CI workflow
      contains: "astral-sh/setup-uv@v8"
    - path: .github/workflows/deploy.yml
      provides: Docker build+push workflow
      contains: "docker/build-push-action@v7"
    - path: tests/test_ci_workflow.py
      provides: CI workflow contract tests
      contains: "WORKFLOW_PATH"
    - path: tests/test_deploy_workflow.py
      provides: Deploy workflow contract tests
      contains: "deploy.yml"
    - path: README.md
      provides: Two-workflow CI/CD documentation
      contains: "deploy.yml"
  key_links:
    - from: .github/workflows/ci.yml
      to: uv/ruff/pytest
      via: lint+test steps only
      pattern: "ruff check"
    - from: .github/workflows/deploy.yml
      to: ghcr.io
      via: metadata-action and build-push-action
      pattern: "ghcr.io/\\$\\{\\{ github.repository \\}\\}"
    - from: tests/test_ci_workflow.py
      to: .github/workflows/ci.yml
      via: static contract assertions
      pattern: "WORKFLOW_PATH"
    - from: tests/test_deploy_workflow.py
      to: .github/workflows/deploy.yml
      via: static contract assertions
      pattern: "deploy.yml"
---

<objective>
Split the monolithic `.github/workflows/ci.yml` into `ci.yml` (lint+test) and `deploy.yml` (Docker build+GHCR push), superseding Phase 4 D-11 while preserving D-01–D-10 and D-12–D-14 behavior across both files.

Purpose: PRs get fast quality feedback without Docker cost; main pushes publish images in parallel with CI.
Output: Two workflow files, updated contract tests, updated README.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo/260709-n8y-CONTEXT.md
@.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md
@.github/workflows/ci.yml
@tests/test_ci_workflow.py
@README.md
@pyproject.toml

<interfaces>
Current ci.yml single job (lines 17–77) splits as:

**ci.yml retains:** checkout@v7, setup-uv@v8.3.2 (python 3.12, enable-cache), CPU torch two-step install, ruff check/format, pytest -m "not docker and not compose". Triggers: push+pull_request to main. Permissions: contents:read only. Concurrency: ${{ github.workflow }}-${{ github.ref }}.

**deploy.yml receives:** checkout@v7, setup-buildx-action@v4, read version from pyproject.toml, login-action@v4, metadata-action@v6 (ghcr.io/${{ github.repository }}, SHA/latest/semver tags per D-08), build-push-action@v7 (push true on main-only workflow, cache-from/cache-to type=gha). Trigger: push to main only. Permissions: contents:read + packages:write. Same concurrency pattern.

**Supersedes D-11:** No single-job assertion; PRs no longer Docker-build (partial D-02 override per quick CONTEXT).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split ci.yml and create deploy.yml</name>
  <files>.github/workflows/ci.yml, .github/workflows/deploy.yml</files>
  <read_first>
    - .github/workflows/ci.yml — copy exact pins and step bodies before editing
    - .planning/quick/260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo/260709-n8y-CONTEXT.md — workflow split, triggers, permissions, concurrency
    - .planning/phases/04-ci-cd-pipeline/04-CONTEXT.md — D-07, D-08, D-12, D-13 preserved in deploy
  </read_first>
  <action>
    Create `.github/workflows/deploy.yml` (filename per quick CONTEXT discretion):

    - `name: Deploy` (or equivalent clear name)
    - `on.push.branches: [main]` only — no `pull_request`, no `workflow_run` dependency on ci.yml
    - `concurrency`: `group: ${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: true` (D-03 per file)
    - `permissions`: `contents: read`, `packages: write`
    - Single job (e.g. `deploy`) with steps moved verbatim from current ci.yml lines 47–77: checkout@v7, docker/setup-buildx-action@v4, Read version (pyproject.toml tomllib), docker/login-action@v4 (ghcr.io + GITHUB_TOKEN), docker/metadata-action@v6 (images ghcr.io/${{ github.repository }}, tags SHA short/latest/semver with main enable expressions), docker/build-push-action@v7 (push: true — workflow only fires on main push; preserving `push: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}` is also acceptable per D-02/D-12), cache-from/cache-to type=gha

    Slim `.github/workflows/ci.yml`:

    - Keep `name: CI`, triggers (push+pull_request to main per D-01), concurrency block unchanged pattern
    - Change `permissions` to `contents: read` only — remove `packages: write`
    - Keep job `ci` with checkout, setup-uv@v8.3.2, CPU torch install, Lint, Test steps only
    - Remove all Docker-related steps (setup-buildx, read version, login, metadata, build-push)

    Do not change Dockerfile, application code, or add werf/compose E2E. Preserve action version pins exactly.
  </action>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .github/workflows/deploy.yml && ! grep -q 'docker/' .github/workflows/ci.yml && grep -q 'astral-sh/setup-uv@v8' .github/workflows/ci.yml && grep -q 'docker/build-push-action@v7' .github/workflows/deploy.yml && grep -q 'pull_request' .github/workflows/ci.yml && ! grep -q 'pull_request' .github/workflows/deploy.yml && grep -q 'packages: write' .github/workflows/deploy.yml && ! grep -q 'packages: write' .github/workflows/ci.yml</automated>
  </verify>
  <done>Two workflow files exist; ci.yml is lint+test only with contents:read; deploy.yml is Docker+GHCR with main-only trigger and packages:write.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite contract tests for two-workflow layout</name>
  <files>tests/test_ci_workflow.py, tests/test_deploy_workflow.py</files>
  <read_first>
    - tests/test_ci_workflow.py — existing D-01..D-14 assertions to remap
    - .github/workflows/ci.yml and .github/workflows/deploy.yml — post-split contents
    - .planning/quick/260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo/260709-n8y-CONTEXT.md — replace test_d11_single_ci_job
  </read_first>
  <action>
    Supersede D-11 single-job test. Split contract coverage across two modules (per discretion):

    **tests/test_ci_workflow.py** — CI-only assertions:
    - Keep/update: file exists, D-01 triggers (push+PR to main), D-03 concurrency, D-07/D-14 setup-uv, D-07 CPU torch install, D-05/D-06 ruff before pytest, D-04 pytest marker exclusion, no werf/compose/docker-scripts
    - Replace `test_d11_single_ci_job` with `test_two_workflow_layout_ci_has_single_ci_job`: assert exactly one job named `ci`; assert no docker/buildx/login/metadata/build-push action strings in ci.yml
    - Replace `test_permissions_contents_read_packages_write` with `test_ci_permissions_contents_read_only`: contents:read present, packages:write absent
    - Remove docker-specific tests (D-02/D-12 push gate, D-13 cache, D-08/D-09 tags, buildx/login) from this file

    **tests/test_deploy_workflow.py** — new module for deploy.yml:
    - Fixture `DEPLOY_WORKFLOW_PATH = Path(".github/workflows/deploy.yml")`
    - Assert file exists; trigger is push to main only (no pull_request)
    - D-03 concurrency in deploy file
    - permissions: contents:read + packages:write
    - Single deploy job (not quality-gate/build-and-push split names)
    - D-02/D-12: build-push-action present; push gated to main (expression or implicit via trigger-only)
    - D-13: cache-from type=gha, cache-to type=gha,mode=max
    - D-08/D-09: ghcr.io/${{ github.repository }}, SHA/latest/semver metadata tags
    - D-12: setup-buildx@v4, login@v4, metadata@v6, checkout@v7
    - Assert no ruff/pytest/setup-uv steps in deploy.yml
    - no werf/compose/docker-scripts in deploy.yml

    Update module docstrings to reference both workflows. Run full pytest suite after edits.
  </action>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_ci_workflow.py tests/test_deploy_workflow.py -q</automated>
  </verify>
  <done>Contract tests pass; D-11 replaced with two-workflow layout tests; D-01–D-14 behavior covered across ci and deploy modules.</done>
</task>

<task type="auto">
  <name>Task 3: Update README CI/CD for two workflows</name>
  <files>README.md</files>
  <read_first>
    - README.md — existing ## CI/CD section (lines 118–187)
    - .planning/quick/260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo/260709-n8y-CONTEXT.md — PR vs main behavior
    - .github/workflows/ci.yml and deploy.yml — final triggers
  </read_first>
  <action>
    Update `## CI/CD` to document two workflows without rewriting Docker/observability sections:

    - Link both `.github/workflows/ci.yml` and `.github/workflows/deploy.yml`
    - **Triggers:** ci.yml on push to main + PRs targeting main; deploy.yml on push to main only; feature-branch pushes without PR still do not trigger
    - **PR behavior:** ci.yml only (lint + test); no Docker build, no GHCR push
    - **Main push:** ci.yml (lint+test) and deploy.yml (build+push) run in parallel — no workflow_run gate
    - Replace "single job in order" wording with split: CI job steps 1–2 (lint, test); deploy job steps (build, push on main)
    - Preserve GHCR image name, tag table, pull examples, local `basic-model-serving:local`, one-time Public visibility, no K8s/werf deploy notes
    - State explicitly that deploy does not run on pull requests

    Minor section structure/wording at executor discretion.
  </action>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -q 'deploy.yml' README.md && grep -q '.github/workflows/ci.yml' README.md && grep -qiE 'pull request|PR' README.md && grep -q 'ghcr.io/estevaodr/basic-model-serving' README.md && grep -qiE 'parallel|deploy' README.md && grep -q '## Docker' README.md</automated>
  </verify>
  <done>README accurately describes two-workflow CI/CD and PR vs main behavior per quick CONTEXT.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR → ci.yml | Untrusted fork/branch code runs lint+test only; no packages:write |
| main → deploy.yml | Trusted branch builds and pushes container image to GHCR |
| GITHUB_TOKEN → GHCR | Scoped via workflow permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | ci.yml permissions | mitigate | contents:read only on ci.yml; packages:write confined to deploy.yml |
| T-04-02 | Tampering | PR Docker build surface | mitigate | Remove Docker steps from PR path; deploy.yml has no pull_request trigger |
| T-04-07 | Elevation of Privilege | GHCR push from forks | mitigate | deploy.yml main-only trigger; push gate preserved in build-push-action |
| T-04-09 | Spoofing | Wrong image in docs | mitigate | README keeps exact ghcr.io/estevaodr/basic-model-serving |
| T-04-SC | Tampering | Workflow action pins | accept | Preserve existing pinned majors; no new package installs |
</threat_model>

<verification>
- Workflow split grep gates pass (Task 1)
- `pytest tests/test_ci_workflow.py tests/test_deploy_workflow.py` passes (Task 2)
- README documents both workflows and PR-only CI behavior (Task 3)
</verification>

<success_criteria>
- ci.yml: lint+test on push/PR to main; no Docker steps; contents:read only
- deploy.yml: Docker build+GHCR push on main push only; packages:write; D-08/D-12/D-13 preserved
- Contract tests replace D-11 and cover D-01–D-14 across both files
- README reflects two-workflow parallel main-push behavior
</success_criteria>

<output>
Create `.planning/quick/260709-n8y-split-ci-into-ci-yml-lint-test-and-deplo/260709-n8y-SUMMARY.md` when done
</output>
