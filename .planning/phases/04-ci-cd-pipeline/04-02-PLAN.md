---
phase: 04-ci-cd-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - README.md
autonomous: false
requirements:
  - CI-01
  - CI-03
  - CI-04
user_setup:
  - service: ghcr
    why: "First main push creates the package private by default; D-10 requires public visibility for anonymous docker pull"
    dashboard_config:
      - task: "After first successful main CI push, set package visibility to Public"
        location: "GitHub → Packages → basic-model-serving → Package settings → Danger Zone → Change visibility → Public"

must_haves:
  truths:
    - "README documents how CI triggers, what it runs, and that only main pushes publish to GHCR"
    - "README documents GHCR image name and tags (short SHA, latest, bare semver) plus one-time Public visibility steps"
    - "After merge to main, a warm-cache CI run completes in under 10 minutes"
    - "Anonymous docker pull of the public GHCR image works after the one-time visibility change"
  artifacts:
    - path: README.md
      provides: CI/CD and GHCR reviewer documentation
      contains: "ghcr.io/estevaodr/basic-model-serving"
    - path: .github/workflows/ci.yml
      provides: Live pipeline verified on GitHub Actions (from 04-01)
      contains: "packages: write"
  key_links:
    - from: README.md
      to: ghcr.io/estevaodr/basic-model-serving
      via: documented pull examples
      pattern: "ghcr.io/estevaodr/basic-model-serving"
    - from: README.md
      to: Package settings Danger Zone
      via: one-time Public visibility steps (D-10)
      pattern: "Change visibility"
---

## Phase Goal

**As a** developer, **I want to** push to `main` (or open a PR targeting `main`) and get automated lint, test, and Docker build with GHCR publish on main, **so that** every change is quality-gated and a reviewable image is published without manual release steps.

<objective>
Close the CI vertical slice for reviewers: document the pipeline and GHCR pull story, then human-verify first main publish, public package visibility (D-10), and warm-run &lt;10 min (CI-04).

Purpose: CI-01/CI-03/CI-04 observable in GitHub + README; D-10 cannot be automated until the package exists.
Output: README CI/GHCR section; verified Actions run + public package.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ci-cd-pipeline/04-CONTEXT.md
@.planning/phases/04-ci-cd-pipeline/04-RESEARCH.md
@.planning/phases/04-ci-cd-pipeline/04-01-SUMMARY.md
@.github/workflows/ci.yml
@README.md
@pyproject.toml

<interfaces>
README CI section must document:
- Triggers: push to main + PRs targeting main (D-01)
- PR path: lint + test + Docker build, no push (D-02)
- Main path: lint + test + build + push to GHCR (D-02)
- Image: `ghcr.io/estevaodr/basic-model-serving`
- Tags on main: short SHA (7), `latest`, bare semver from pyproject (currently `0.1.0`) — no `v` prefix
- Local compose still uses `basic-model-serving:local` (unchanged)
- One-time GHCR Public visibility UI steps after first successful main push (D-10) — not automated in workflow
- Explicit note: CI does not deploy to Kubernetes / no werf converge from Actions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document CI/CD and GHCR in README</name>
  <files>README.md</files>
  <read_first>
    - README.md — existing Docker and Local observability sections (preserve; add new section)
    - .github/workflows/ci.yml — exact triggers, tags, image name
    - .planning/phases/04-ci-cd-pipeline/04-CONTEXT.md — D-01, D-02, D-08, D-09, D-10
    - .planning/phases/04-ci-cd-pipeline/04-RESEARCH.md — GHCR public visibility UI steps
    - pyproject.toml — current version string for example tag
  </read_first>
  <action>
    Add a README section titled `## CI/CD` (or `## Continuous integration`) after the existing Docker / observability content without rewriting those sections.

    Document per D-01/D-02: workflow file `.github/workflows/ci.yml`; runs on push to `main` and pull_request targeting `main`; PRs lint+test+build without push; main push also publishes to GHCR.

    Document per D-08/D-09: image `ghcr.io/estevaodr/basic-model-serving`; tags short SHA, `latest`, and bare semver (example `0.1.0` from pyproject — no `v` prefix). Include example `docker pull` lines for `latest` and a SHA/semver form.

    Document per D-10: after the first successful main push, open the package on GitHub → Package settings → Danger Zone → Change visibility → Public (irreversible). State that packages default to private even when the repo is public.

    State clearly: local/dev compose continues to use `basic-model-serving:local`; CI does not run `werf converge` or deploy to minikube.

    Do not invent secrets or require a PAT for anonymous pull after Public visibility.
  </action>
  <acceptance_criteria>
    - README.md contains a CI/CD section heading
    - README mentions `.github/workflows/ci.yml`
    - README contains `ghcr.io/estevaodr/basic-model-serving`
    - README mentions `latest` and bare semver tag `0.1.0` (or notes version comes from pyproject.toml)
    - README documents Change visibility / Public (or Danger Zone) for GHCR
    - README states PRs do not push images (or equivalent: push only on main)
    - README states CI does not deploy to Kubernetes / no werf from Actions
    - Existing `## Docker` and `## Local observability stack` sections remain present
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -qE '^## CI' README.md && grep -q '.github/workflows/ci.yml' README.md && grep -q 'ghcr.io/estevaodr/basic-model-serving' README.md && grep -q 'latest' README.md && grep -q '0.1.0' README.md && grep -qiE 'Change visibility|Danger Zone|visibility.*Public' README.md && grep -qiE 'pull_request|PR' README.md && grep -qiE 'werf|Kubernetes|minikube' README.md && grep -q '## Docker' README.md && grep -q '## Local observability stack' README.md</automated>
  </verify>
  <done>Reviewers can follow README for CI behavior and GHCR pull/visibility (D-01, D-02, D-08, D-09, D-10).</done>
</task>

<task type="auto">
  <name>Task 2: Push branch and confirm Actions run exists</name>
  <files>.github/workflows/ci.yml, README.md</files>
  <read_first>
    - .github/workflows/ci.yml
    - README.md — CI section from Task 1
    - .planning/phases/04-ci-cd-pipeline/04-01-SUMMARY.md — confirm workflow committed
  </read_first>
  <action>
    Ensure 04-01 workflow + this plan's README changes are on a branch that GitHub Actions can see.

    If not already pushed: commit only if the user/orchestrator already authorized commits for this phase execution; otherwise push existing commits and open/update a PR targeting `main` so a `pull_request` workflow run fires (CI-01/D-01).

    Use `gh run list --workflow=ci.yml --limit 5` (or `gh run list --workflow=CI`) to confirm at least one run was queued/completed for this branch or PR.

    Do not force-push. Do not change workflow permissions. Do not attempt to set package visibility via API unless the human later asks — UI path is locked by RESEARCH discretion.

    If Actions is disabled on the fork/repo, record the blocker in SUMMARY and stop before the human checkpoint with clear remediation (enable Actions).
  </action>
  <acceptance_criteria>
    - `gh run list --workflow=ci.yml --limit 5` (or equivalent workflow name filter) shows at least one run for this repo related to the CI workflow
    - Remote contains `.github/workflows/ci.yml`
    - No workflow file changes that reintroduce a two-job split
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .github/workflows/ci.yml && test "$(gh run list --workflow=ci.yml --limit 5 --json databaseId --jq 'length')" -ge 1</automated>
  </verify>
  <done>CI-01 observable: GitHub has queued/executed the workflow for this change set.</done>
</task>

<task type="checkpoint:human">
  <name>Task 3: Verify main publish, public GHCR, and warm &lt;10 min</name>
  <files>README.md</files>
  <read_first>
    - README.md — CI/CD and GHCR Public visibility steps
    - .planning/phases/04-ci-cd-pipeline/04-RESEARCH.md — Pitfall 4 (private default) and CI-04 warm-run guidance
    - .planning/phases/04-ci-cd-pipeline/04-CONTEXT.md — D-10, CI-04
  </read_first>
  <action>
    Complete the human-only verification path for CI-03, CI-04, and D-10:

    1. Merge or push to `main` so a main `push` run executes build+push (if only a PR ran so far).
    2. Open the Actions run for that main push; confirm lint → test → build → push succeeded.
    3. Confirm GHCR tags exist for short SHA, `latest`, and bare semver (`0.1.0` or current pyproject version).
    4. Perform the README one-time Package settings → Danger Zone → Change visibility → Public (D-10).
    5. From a shell without GHCR auth (or `docker logout ghcr.io`), run `docker pull ghcr.io/estevaodr/basic-model-serving:latest` and confirm it succeeds.
    6. For CI-04: on a subsequent main run with warm GHA cache (second successful build after cache populate), confirm Actions reports total duration under 10 minutes. First cold run may be borderline — record cold vs warm durations in SUMMARY; CI-04 acceptance is the warm run.

    Resume with "approved" or list failures (403 push, private pull, duration &gt;10m warm).
  </action>
  <acceptance_criteria>
    - Main branch Actions run green with image push
    - Package visibility is Public
    - Anonymous `docker pull ghcr.io/estevaodr/basic-model-serving:latest` succeeds
    - Warm-cache pipeline duration &lt; 10 minutes (CI-04)
  </acceptance_criteria>
  <verify>
    <human>Operator confirms green main push, Public package, anonymous pull, and warm run under 10 minutes</human>
  </verify>
  <done>CI-01/CI-03/CI-04 and D-10 verified in production GitHub/GHCR (must_have truths gated at execution).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → reviewer machine | Documented pull commands execute against public GHCR |
| GitHub Packages settings UI | Human admin changes package visibility (irreversible Public) |
| Actions → GHCR | Already mitigated in 04-01; this plan verifies outcomes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Information Disclosure | Public GHCR package (D-10) | accept | Intentional portfolio exposure; README documents irreversibility; no secrets in image |
| T-04-07 | Elevation of Privilege | Accidental PAT in README | mitigate | Document anonymous pull after Public only; never instruct creating a PAT for routine pull; CI continues to use ambient GITHUB_TOKEN only |
| T-04-08 | Spoofing | Typosquatted image name in docs | mitigate | README must use exact `ghcr.io/estevaodr/basic-model-serving` matching `github.repository` |
| T-04-09 | Tampering | Documenting push from forks/PRs | mitigate | README states push only on main; aligns with workflow gate from 04-01 |
| T-04-SC | Tampering | No new package installs in this plan | accept | README-only + human verification; no dependency changes |
</threat_model>

<verification>
- README CI/GHCR section grep gates pass
- gh run list shows workflow activity
- Human checkpoint: Public package + anonymous pull + warm &lt;10 min
</verification>

<success_criteria>
- CI-01: Observed Actions runs on main/PR
- CI-03: Image on GHCR with usable tags; Public pull works (D-09, D-10)
- CI-04: Warm-cache run under 10 minutes
- Reviewer-ready README for CI/GHCR story
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `README.md` CI/CD section | 04-02 | Triggers, PR vs main, GHCR tags, Public visibility steps |
| Documented image `ghcr.io/estevaodr/basic-model-serving` | 04-02 | Reviewer pull target (D-09) |
| Documented tags: short SHA, `latest`, bare semver | 04-02 | Matches D-08 publish contract |
| Human-verified Public GHCR package | 04-02 | D-10 one-time UI (not in workflow) |
| Warm-run duration evidence (&lt;10 min) | 04-02 | CI-04 acceptance |

<output>
Create `.planning/phases/04-ci-cd-pipeline/04-02-SUMMARY.md` when done
</output>
