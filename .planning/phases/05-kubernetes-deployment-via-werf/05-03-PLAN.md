---
phase: 05-kubernetes-deployment-via-werf
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - scripts/rollout-zero-downtime.sh
  - .helm/values-local.yaml
  - README.md
  - tests/test_helm_chart.py
autonomous: false
requirements:
  - K8S-05
  - K8S-06
user_setup: []

must_haves:
  truths:
    - "README documents minikube sizing, GHCR SHA converge, and local image load converge paths"
    - "scripts/rollout-zero-downtime.sh hammers /health/ready during rollout without sustained 503/000"
    - "Rolling update with 2 replicas and maxUnavailable 0 keeps at least one ready endpoint during image change"
    - "CI deploy boundary preserved — no werf step in GitHub Actions"
  artifacts:
    - path: scripts/rollout-zero-downtime.sh
      provides: Curl loop readiness hammer during rollout
      min_lines: 15
    - path: .helm/values-local.yaml
      provides: Local tag image overrides for minikube image load path
    - path: README.md
      provides: Kubernetes (minikube + werf) section with converge and access commands
      contains: "werf converge"
  key_links:
    - from: README.md
      to: scripts/rollout-zero-downtime.sh
      via: zero-downtime demo instructions
      pattern: "rollout-zero-downtime"
    - from: scripts/rollout-zero-downtime.sh
      to: minikube service model-serving
      via: API_URL from minikube service --url
      pattern: "minikube service model-serving"
    - from: README.md
      to: .github/workflows/deploy.yml
      via: CI stops at build+push boundary
      pattern: "does not deploy"
---

## Phase Goal

**As a** developer, **I want to** deploy the model-serving API to minikube via `werf converge` with self-healing probes and configurable env vars, **so that** the service runs orchestrated in local Kubernetes exactly as it would in production.

<objective>
Deliver MVP vertical slice 3: zero-downtime rolling update proof (K8S-05) and complete deploy documentation for GHCR SHA and local iteration paths (D-05, D-06, D-16, D-19, D-20).

Purpose: Close the phase with a human-runnable rollout demo and reviewer-ready README commands; preserve CI→werf manual boundary.
Output: `scripts/rollout-zero-downtime.sh`, `.helm/values-local.yaml`, README Kubernetes section, extended contract tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-kubernetes-deployment-via-werf/05-CONTEXT.md
@.planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md
@.planning/phases/05-kubernetes-deployment-via-werf/05-PATTERNS.md
@README.md
@scripts/docker.py
@.github/workflows/deploy.yml
@tests/test_deploy_workflow.py

<interfaces>
Documentation and rollout contract:
- Minikube start: minikube start --driver=docker --cpus=4 --memory=8192 --disk-size=20g (D-16)
- GHCR converge: werf converge --env local --dev --without-images --set image.repository=ghcr.io/estevaodr/basic-model-serving --set image.tag=$(git rev-parse --short HEAD) (D-05)
- Local converge: uv run docker-build → minikube image load basic-model-serving:local → werf converge with values-local.yaml or --set overrides (D-06, D-20)
- API access: minikube service model-serving --url (D-09)
- Grafana access: minikube service prometheus-stack-grafana -n <namespace> --url (D-10, fullnameOverride from 05-02)
- Rollout script: curl loop on /health/ready every 0.5s; success = no sustained 503/000 streak (D-19)
- README must state CI does not run werf (manual deploy boundary)
- values-local.yaml: image.repository basic-model-serving, tag local, pullPolicy IfNotPresent
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rollout script, values-local, and README contract tests</name>
  <files>scripts/rollout-zero-downtime.sh, .helm/values-local.yaml, README.md, tests/test_helm_chart.py</files>
  <read_first>
    - .planning/phases/05-kubernetes-deployment-via-werf/05-CONTEXT.md — D-05, D-06, D-16, D-19, D-20
    - .planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md — Pattern 6 curl proof, local converge command
    - .planning/phases/05-kubernetes-deployment-via-werf/05-PATTERNS.md — rollout-zero-downtime.sh analog
    - README.md — existing Docker and CI sections to preserve
    - scripts/docker.py — _wait_ready polling pattern
  </read_first>
  <behavior>
    - test_rollout_script_exists fails until scripts/rollout-zero-downtime.sh exists
    - test_readme_k8s_section fails until README contains werf converge and minikube start sizing
    - test_values_local_exists fails until .helm/values-local.yaml exists with local tag
    - test_deploy_workflow_no_werf still passes (CI boundary)
  </behavior>
  <action>
    Extend tests/test_helm_chart.py (or add tests/test_k8s_docs.py if cleaner) with documentation and script contract tests:
    - Assert README contains substring "Kubernetes" or "minikube + werf"
    - Assert README contains minikube start --cpus=4 --memory=8192 (D-16)
    - Assert README contains werf converge --without-images (D-05, K8S-06)
    - Assert README contains minikube service model-serving (D-09)
    - Assert README contains minikube image load basic-model-serving:local (D-06)
    - Assert README states CI does not deploy to Kubernetes / no werf in Actions
    - Assert scripts/rollout-zero-downtime.sh exists and contains /health/ready and minikube service

    Create scripts/rollout-zero-downtime.sh per RESEARCH Pattern 6: bash, API_URL from minikube service model-serving --url, curl loop every 0.5s printing timestamp and HTTP code (D-19). Add executable bit in git.

    Create .helm/values-local.yaml with image.repository basic-model-serving, tag local, pullPolicy IfNotPresent (D-06).

    Add README section ## Kubernetes (minikube + werf) after CI/GHCR section documenting:
    1. Prerequisites and minikube start command (D-16)
    2. GHCR SHA converge command (D-05)
    3. Local iteration path (D-06): docker-build, image load, converge with values-local.yaml
    4. Access: minikube service model-serving and prometheus-stack-grafana (D-09, D-10)
    5. Zero-downtime demo: run rollout script in terminal 1, trigger converge with new local tag in terminal 2 (D-19, D-20)
    6. Boundary: CI build+push only; manual werf converge from laptop

    Preserve existing README Docker, compose, and CI sections unchanged.
  </action>
  <acceptance_criteria>
    - scripts/rollout-zero-downtime.sh exists and is executable
    - .helm/values-local.yaml contains tag: local and pullPolicy: IfNotPresent
    - README contains werf converge, minikube start --memory=8192, and rollout-zero-downtime reference
    - Documentation contract tests pass
    - tests/test_deploy_workflow.py still passes (no werf in deploy.yml)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -x scripts/rollout-zero-downtime.sh && test -f .helm/values-local.yaml && grep -q 'werf converge' README.md && grep -q 'memory=8192' README.md && grep -q 'rollout-zero-downtime' README.md && pytest tests/test_helm_chart.py tests/test_deploy_workflow.py -q</automated>
  </verify>
  <done>Rollout script, local values, and README k8s docs exist with contract test coverage.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify werf converge and zero-downtime rollout on minikube</name>
  <files>README.md, scripts/rollout-zero-downtime.sh, .helm/values-local.yaml</files>
  <read_first>
    - README.md — Kubernetes section from Task 1
    - scripts/rollout-zero-downtime.sh — curl loop script
    - .helm/values-local.yaml — local image overrides
    - .planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md — Pattern 6 proof criteria
  </read_first>
  <what-built>
    Full Phase 5 stack: werf.yaml, .helm chart with app + kube-prometheus-stack, rollout script, and README deploy commands. Automated contract tests are green.
  </what-built>
  <action>
    Pause for human verification on a live minikube cluster. Present the user with the exact steps below. Do not mark this task complete until the user types "approved" or reports specific failures to fix.

    Verification steps:
    1. Start minikube: minikube start --driver=docker --cpus=4 --memory=8192 --disk-size=20g
    2. Build and load local image: uv run docker-build then minikube image load basic-model-serving:local
    3. Initial deploy: werf converge --env local --dev --without-images -f .helm/values-local.yaml --set image.repository=basic-model-serving --set image.tag=local --set image.pullPolicy=IfNotPresent
    4. Verify API: minikube service model-serving --url then curl /health/ready returns 200
    5. Verify Grafana: minikube service prometheus-stack-grafana -n basic-model-serving-local --url opens UI; Model Serving Overview dashboard visible
    6. Terminal 1: run ./scripts/rollout-zero-downtime.sh
    7. Terminal 2: rebuild image (uv run docker-build), minikube image load basic-model-serving:local, re-run werf converge with same local flags
    8. Confirm curl loop shows no sustained 503/000 streak (transient blips OK per D-19)
  </action>
  <acceptance_criteria>
    - Both pods reach Running/Ready after initial converge
    - /health/ready returns 200 via minikube service URL
    - Grafana dashboard shows request metrics after a few /predict or /health requests
    - Rollout curl loop completes without sustained outage during image reload converge
  </acceptance_criteria>
  <verify>
    <human-check>User confirms minikube converge, API/Grafana access, and zero-downtime rollout per steps above; resume signal "approved"</human-check>
  </verify>
  <done>Human approved live minikube deploy and zero-downtime rollout demo (K8S-05).</done>
  <how-to-verify>
    See action steps 1–8 above.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found during minikube verification</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize contract tests and CI-quality gate</name>
  <files>tests/test_helm_chart.py</files>
  <read_first>
    - tests/test_helm_chart.py — full suite
    - .github/workflows/ci.yml — pytest marker filter
  </read_first>
  <action>
    After human approval, ensure full test suite passes:

    Run ruff check . and ruff format --check . on any files touched in this plan.

    Run pytest -m "not docker and not compose" including all helm chart and deploy workflow contract tests.

    If human verification found issues, fix chart values, README commands, or rollout script and re-run tests before marking complete.
  </action>
  <acceptance_criteria>
    - ruff check . exits 0
    - pytest -m "not docker and not compose" exits 0
    - All phase requirement IDs K8S-01 through K8S-06 and PERF-04 have implementing artifacts across plans 05-01 through 05-03
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && ruff check . && ruff format --check . && pytest -m "not docker and not compose" -q</automated>
  </verify>
  <done>Phase 5 complete: contract tests green, human verified zero-downtime rollout on minikube.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → minikube during rollout demo | Manual image load and converge from laptop |
| README → reviewer | Public deploy instructions; no secrets in docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Accidental CI werf deploy | mitigate | test_deploy_workflow.py asserts no werf in deploy.yml; README states manual boundary |
| T-05-09 | Denial of Service | Rollout script masks real outage | mitigate | Human checkpoint verifies curl loop output; script prints every response code (D-19) |
| T-05-10 | Spoofing | Wrong namespace in minikube service | accept | README documents namespace basic-model-serving-local; human verify step uses explicit -n flag |
| T-05-SC | Tampering | Shell script execution | accept | rollout script uses curl/minikube only; no curl\|bash from remote URLs |
</threat_model>

<verification>
- Documentation contract tests pass
- Human approved minikube converge + zero-downtime rollout
- Full pytest marker-filtered suite passes
</verification>

<success_criteria>
- K8S-05: 2 replicas + maxUnavailable 0 + curl-loop proof demonstrates zero sustained downtime during rollout (D-17, D-18, D-19, D-20)
- K8S-06: README documents manual werf converge from laptop; CI unchanged
- D-05: GHCR SHA converge documented
- D-06: Local docker-build + minikube image load path documented
- D-16: minikube sizing command documented
- Phase goal complete: orchestrated, self-healing, configurable, zero-downtime deploy with bundled monitoring
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `scripts/rollout-zero-downtime.sh` | 05-03 | Curl loop on /health/ready during rollout (D-19) |
| `.helm/values-local.yaml` | 05-03 | Local image tag overrides for minikube image load (D-06) |
| `README.md` (K8s section) | 05-03 | minikube sizing, converge commands, access URLs, rollout demo (D-05, D-16, D-20) |
| Human verification record | 05-03 | minikube converge + zero-downtime rollout approved |
| Extended `tests/test_helm_chart.py` | 05-03 | README and script contract tests |

<output>
Create `.planning/phases/05-kubernetes-deployment-via-werf/05-03-SUMMARY.md` when done
</output>
