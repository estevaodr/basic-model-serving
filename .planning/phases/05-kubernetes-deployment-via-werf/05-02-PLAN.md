---
phase: 05-kubernetes-deployment-via-werf
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .helm/Chart.yaml
  - .helm/Chart.lock
  - .helm/values.yaml
  - .helm/templates/servicemonitor.yaml
  - .helm/templates/grafana-dashboard.yaml
  - .helm/dashboards/model-serving-overview.json
  - .gitignore
  - tests/test_helm_chart.py
autonomous: true
requirements:
  - K8S-06
user_setup: []

must_haves:
  truths:
    - "Single werf converge deploys API and kube-prometheus-stack together"
    - "ServiceMonitor with release label matching werf Release.Name enables Prometheus scrape of /metrics"
    - "Grafana shows Model Serving Overview dashboard with live request_count data after traffic"
    - "minikube service exposes Grafana UI via NodePort"
  artifacts:
    - path: .helm/Chart.yaml
      provides: kube-prometheus-stack Helm dependency
      contains: "kube-prometheus-stack"
    - path: .helm/Chart.lock
      provides: Pinned subchart versions
    - path: .helm/templates/servicemonitor.yaml
      provides: Operator scrape config for /metrics
      contains: "release: {{ .Release.Name }}"
    - path: .helm/templates/grafana-dashboard.yaml
      provides: Sidecar-labeled dashboard ConfigMap
      contains: "grafana_dashboard"
    - path: .helm/dashboards/model-serving-overview.json
      provides: Phase 3 dashboard JSON copy
      min_lines: 50
  key_links:
    - from: .helm/templates/servicemonitor.yaml
      to: .helm/templates/service.yaml
      via: selector matchLabels app.kubernetes.io/name model-serving
      pattern: "model-serving"
    - from: kube-prometheus-stack subchart
      to: .helm/templates/servicemonitor.yaml
      via: release label selector
      pattern: "Release.Name"
    - from: .helm/templates/grafana-dashboard.yaml
      to: monitoring/grafana/dashboards/model-serving-overview.json
      via: .Files.Get dashboards copy
      pattern: "model-serving-overview"
---

## Phase Goal

**As a** developer, **I want to** deploy the model-serving API to minikube via `werf converge` with self-healing probes and configurable env vars, **so that** the service runs orchestrated in local Kubernetes exactly as it would in production.

<objective>
Deliver MVP vertical slice 2: extend the 05-01 chart so one `werf converge` also installs kube-prometheus-stack, scrapes `/metrics` via ServiceMonitor, and provisions the Phase 3 Model Serving Overview dashboard in Grafana.

Purpose: Implement locked monitoring decisions D-01 through D-04 and D-10; complete the hero single-converge workflow.
Output: kube-prometheus-stack subchart wiring, ServiceMonitor, Grafana dashboard ConfigMap, Chart.lock, `.gitignore` update.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-kubernetes-deployment-via-werf/05-CONTEXT.md
@.planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md
@.planning/phases/05-kubernetes-deployment-via-werf/05-PATTERNS.md
@monitoring/grafana/dashboards/model-serving-overview.json
@app/metrics/prometheus.py
@.planning/phases/03-local-dev-stack-dashboards/03-CONTEXT.md

<interfaces>
Monitoring contract (D-01 through D-04, D-10):
- Chart dependency: kube-prometheus-stack version ~87.12.0 from prometheus-community repo
- Subchart values: prometheus.prometheusSpec.retention 7d, serviceMonitorSelectorNilUsesHelmValues true
- Subchart values: grafana.service.type NodePort; sidecar.dashboards enabled label grafana_dashboard labelValue "1"
- Subchart values: fullnameOverride prometheus-stack for stable Service names prometheus-stack-grafana (resolves RESEARCH Open Question 1)
- ServiceMonitor labels: release {{ .Release.Name }} — NOT hardcoded prometheus-stack (D-03, Pitfall 1)
- ServiceMonitor selector: app.kubernetes.io/name model-serving; endpoint port http path /metrics interval 15s
- Dashboard ConfigMap: label grafana_dashboard "1", embed JSON via .Files.Get dashboards/model-serving-overview.json
- Copy source: monitoring/grafana/dashboards/model-serving-overview.json (exact copy per D-04)
- Do NOT provision compose alert rules (downtime.yml) — Phase 6 DOC-06
- Run: werf helm dependency update .helm; commit Chart.lock; gitignore .helm/charts/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend contract tests for monitoring templates and subchart</name>
  <files>tests/test_helm_chart.py</files>
  <read_first>
    - tests/test_helm_chart.py — existing app chart tests from 05-01
    - .planning/phases/05-kubernetes-deployment-via-werf/05-CONTEXT.md — D-01 through D-04, D-10
    - .planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md — Patterns 2, 3, 4; Package Legitimacy Audit
    - monitoring/grafana/dashboards/model-serving-overview.json — datasource uid prometheus
  </read_first>
  <behavior>
    - test_chart_has_kube_prometheus_dependency fails until Chart.yaml lists kube-prometheus-stack
    - test_servicemonitor_template_exists fails until servicemonitor.yaml exists
    - Rendered ServiceMonitor contains label release tied to Release.Name (template string, not hardcoded prometheus-stack alone)
    - Rendered ServiceMonitor endpoint path /metrics port http
    - test_grafana_dashboard_configmap fails until grafana-dashboard.yaml exists with grafana_dashboard label
    - test_dashboard_json_embedded fails until .helm/dashboards/model-serving-overview.json exists
    - Chart.lock exists after dependency update
  </behavior>
  <action>
    Extend tests/test_helm_chart.py with monitoring contract tests. Tests MUST fail before Chart.yaml dependency, ServiceMonitor, dashboard templates, and dashboard JSON copy exist.

    Reference D-01, D-02, D-03, D-04 in assertion messages. Assert ServiceMonitor does NOT hardcode release: prometheus-stack without Release.Name template.

    RED only — do not modify Chart.yaml or create monitoring templates yet.
  </action>
  <acceptance_criteria>
    - New monitoring tests exist in tests/test_helm_chart.py
    - pytest tests/test_helm_chart.py -x fails on missing kube-prometheus-stack dependency or servicemonitor template
    - Tests reference D-01, D-03, D-04
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_helm_chart.py -k "prometheus or servicemonitor or grafana or dashboard" -x; test $? -ne 0</automated>
  </verify>
  <done>Failing monitoring contract tests define scrape and dashboard acceptance before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bundle kube-prometheus-stack and implement ServiceMonitor + dashboard</name>
  <files>.helm/Chart.yaml, .helm/Chart.lock, .helm/values.yaml, .helm/templates/servicemonitor.yaml, .helm/templates/grafana-dashboard.yaml, .helm/dashboards/model-serving-overview.json, .gitignore, tests/test_helm_chart.py</files>
  <read_first>
    - tests/test_helm_chart.py — monitoring tests from Task 1
    - .planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md — Pattern 2 subchart values, Pattern 3 ServiceMonitor, Pattern 4 dashboard ConfigMap
    - monitoring/grafana/dashboards/model-serving-overview.json — copy source
    - .helm/values.yaml — existing app image values from 05-01
  </read_first>
  <behavior>
    - All monitoring contract tests pass
    - helm dependency build/update succeeds
    - helm template includes ServiceMonitor and dashboard ConfigMap resources
  </behavior>
  <action>
    Update .helm/Chart.yaml: add dependency kube-prometheus-stack version ~87.12.0 repository https://prometheus-community.github.io/helm-charts (D-01, D-02).

    Run werf helm dependency update .helm (or helm dependency update .helm) to generate Chart.lock. Commit Chart.lock.

    Add .helm/charts/ to .gitignore.

    Extend .helm/values.yaml with kube-prometheus-stack nested values per RESEARCH:
    - prometheus.prometheusSpec.retention: 7d
    - prometheus.prometheusSpec.serviceMonitorSelectorNilUsesHelmValues: true
    - grafana.service.type: NodePort (D-10)
    - grafana.sidecar.dashboards.enabled: true, label grafana_dashboard, labelValue "1"
    - kube-prometheus-stack.fullnameOverride: prometheus-stack (stable minikube service names)

    Create .helm/templates/servicemonitor.yaml per Pattern 3: metadata labels release {{ .Release.Name }} (D-03), selector app.kubernetes.io/name model-serving, endpoint port http path /metrics interval 15s.

    Copy monitoring/grafana/dashboards/model-serving-overview.json to .helm/dashboards/model-serving-overview.json (exact copy, D-04).

    Create .helm/templates/grafana-dashboard.yaml per Pattern 4: ConfigMap with label grafana_dashboard "1", data key model-serving-overview.json from .Files.Get.

    Green monitoring contract tests.
  </action>
  <acceptance_criteria>
    - .helm/Chart.yaml dependencies include kube-prometheus-stack
    - .helm/Chart.lock exists and is non-empty
    - .helm/templates/servicemonitor.yaml contains /metrics and Release.Name
    - .helm/dashboards/model-serving-overview.json exists and matches source panel count (file size within 5% of source)
    - .gitignore contains .helm/charts/
    - pytest tests/test_helm_chart.py -x exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .helm/Chart.lock && grep -q kube-prometheus-stack .helm/Chart.yaml && grep -q '/metrics' .helm/templates/servicemonitor.yaml && grep -q 'Release.Name' .helm/templates/servicemonitor.yaml && grep -q 'grafana_dashboard' .helm/templates/grafana-dashboard.yaml && grep -q '.helm/charts/' .gitignore && pytest tests/test_helm_chart.py -x</automated>
  </verify>
  <done>Single converge chart bundles app + kube-prometheus-stack with ServiceMonitor and dashboard provisioning (D-01, D-02, D-03, D-04).</done>
</task>

<task type="auto">
  <name>Task 3: Validate full chart render and subchart values</name>
  <files>tests/test_helm_chart.py</files>
  <read_first>
    - .helm/values.yaml — full values including subchart section
    - .planning/phases/05-kubernetes-deployment-via-werf/05-RESEARCH.md — Environment Availability table
  </read_first>
  <action>
    Run helm template with --dependency-update if needed; assert rendered manifest kinds include ServiceMonitor and ConfigMap for dashboard.

    Assert values.yaml contains kube-prometheus-stack.grafana.service.type NodePort and prometheus retention 7d.

    Run pytest -m "not docker and not compose" and ruff check tests/test_helm_chart.py.

    Do not add README k8s section (05-03). Do not add rollout script (05-03).
  </action>
  <acceptance_criteria>
    - helm template renders ServiceMonitor kind when helm available
    - values.yaml contains retention: 7d under kube-prometheus-stack prometheus section
    - values.yaml contains type: NodePort under kube-prometheus-stack grafana service
    - pytest -m "not docker and not compose" exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -q 'retention: 7d' .helm/values.yaml && grep -q 'type: NodePort' .helm/values.yaml && ruff check tests/test_helm_chart.py && pytest -m "not docker and not compose" -q</automated>
  </verify>
  <done>Full stack chart renders; monitoring values locked for minikube converge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prometheus Operator → app ServiceMonitor | CR-based scrape discovery in cluster |
| Grafana sidecar → dashboard ConfigMap | Labeled ConfigMap auto-imported |
| Helm chart repo → dependency fetch | prometheus-community official repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | Grafana default admin password | mitigate | Override grafana.adminPassword in subchart values via random or documented dev password in values-local (not committed secret) |
| T-05-06 | Tampering | ServiceMonitor wrong release label | mitigate | Template release {{ .Release.Name }} per D-03; contract test forbids hardcoded-only prometheus-stack |
| T-05-07 | Denial of Service | minikube OOM with full stack | mitigate | Document D-16 minikube sizing 4 CPU 8GB; app resource limits from 05-01 |
| T-05-SC | Tampering | kube-prometheus-stack chart fetch | mitigate | Official prometheus-community helm repo; Chart.lock pins version; Package Legitimacy Audit OK verdict |
</threat_model>

<verification>
- pytest tests/test_helm_chart.py passes including monitoring tests
- Chart.lock committed; .helm/charts/ gitignored
- helm template includes ServiceMonitor and dashboard ConfigMap
</verification>

<success_criteria>
- D-01: kube-prometheus-stack used (not hand-rolled manifests)
- D-02: Bundled as Helm subchart in single werf chart
- D-03: ServiceMonitor release label matches Release.Name
- D-04: Model Serving Overview dashboard provisioned from Phase 3 JSON
- D-10: Grafana NodePort for minikube service access
- K8S-06: Complete werf converge deploys app + monitoring stack
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `.helm/Chart.yaml` (dependency) | 05-02 | kube-prometheus-stack ~87.12.0 subchart (D-01, D-02) |
| `.helm/Chart.lock` | 05-02 | Pinned subchart versions for reproducible converge |
| `.helm/values.yaml` (subchart section) | 05-02 | 7d retention, NodePort Grafana, sidecar dashboards, fullnameOverride |
| `.helm/templates/servicemonitor.yaml` | 05-02 | Prometheus Operator scrape of /metrics (D-03) |
| `.helm/templates/grafana-dashboard.yaml` | 05-02 | Sidecar dashboard ConfigMap (D-04) |
| `.helm/dashboards/model-serving-overview.json` | 05-02 | Copy of Phase 3 dashboard JSON |
| `.gitignore` (.helm/charts/) | 05-02 | Ignore vendored chart tgz; commit Chart.lock |
| Grafana Service `prometheus-stack-grafana` | 05-02 | NodePort for `minikube service` (D-10) |

<output>
Create `.planning/phases/05-kubernetes-deployment-via-werf/05-02-SUMMARY.md` when done
</output>
