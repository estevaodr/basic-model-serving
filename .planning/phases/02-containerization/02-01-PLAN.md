---
phase: 02-containerization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - scripts/__init__.py
  - scripts/docker.py
  - pyproject.toml
  - tests/test_docker_smoke.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
user_setup: []

must_haves:
  truths:
    - "Developer can docker build -t basic-model-serving:local . and produce an image under 2GB"
    - "Container starts as non-root uid 1000 with no permission errors loading baked ResNet-50 weights"
    - "GET /health/ready returns 200 after model load when container is running"
    - "POST /predict with a JPEG returns 200 with exactly 5 predictions"
    - "pip show torch inside the image reports CPU build (+cpu), not CUDA"
  artifacts:
    - path: Dockerfile
      provides: Multi-stage CPU-only build with baked weights and non-root runtime
      contains: "FROM python:3.12-slim AS builder"
    - path: .dockerignore
      provides: Build context exclusions per D-11
      contains: ".planning/"
    - path: scripts/docker.py
      provides: Host-side docker build/run helpers and smoke scaffold
      contains: "basic-model-serving:local"
    - path: pyproject.toml
      provides: uv run entry points for docker-build, docker-run, docker-smoke
      contains: "[project.scripts]"
    - path: tests/test_docker_smoke.py
      provides: Automated gate for container E2E (may skip without Docker)
      min_lines: 20
  key_links:
    - from: Dockerfile
      to: app/models/resnet.py
      via: builder RUN ResNetClassifier() weight download per D-01
      pattern: "ResNetClassifier"
    - from: Dockerfile
      to: app/main.py
      via: CMD uvicorn app.main:app
      pattern: "app.main:app"
    - from: Dockerfile
      to: /health/ready
      via: HEALTHCHECK urllib probe
      pattern: "/health/ready"
---

## Phase Goal

**As a** developer, **I want to** build and run the Phase 1 API inside a secure Docker image, **so that** the service is portable for local dev, CI, and Kubernetes without reconfiguring application code.

<objective>
Deliver a working multi-stage Docker image: CPU-only PyTorch, ResNet-50 weights baked at build time (D-01), non-root runtime (CONT-02), image under 2GB (CONT-01), plus a failing smoke-test scaffold and uv script entry points that Plan 02 completes.

Purpose: First vertical slice — after this plan, `docker build` and `docker run` serve real predictions as non-root.
Output: Dockerfile, .dockerignore, pyproject.toml, scripts/docker.py (build + run), failing smoke test ready for Plan 02 GREEN.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-containerization/02-CONTEXT.md
@.planning/phases/02-containerization/02-RESEARCH.md
@.planning/phases/01-core-inference-api/01-04-SUMMARY.md
@app/main.py
@app/core/config.py
@app/models/resnet.py
@requirements.txt
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing container smoke scaffold and uv script entry points</name>
  <files>scripts/__init__.py, scripts/docker.py, pyproject.toml, tests/test_docker_smoke.py</files>
  <read_first>
    - .planning/phases/02-containerization/02-CONTEXT.md — D-04, D-09, D-10, D-12 locked decisions
    - .planning/phases/02-containerization/02-RESEARCH.md — Pattern 2 uv [project.scripts], smoke assertions
    - tests/conftest.py — sample_jpeg_bytes PIL pattern for minimal JPEG generation
  </read_first>
  <behavior>
    - tests/test_docker_smoke.py defines test_docker_smoke_e2e marked @pytest.mark.docker; test FAILS when Dockerfile absent or smoke preconditions unmet
    - pyproject.toml defines [project.scripts] docker-build, docker-run, docker-smoke mapping to scripts.docker:build|run|smoke
    - scripts/docker.py defines IMAGE = "basic-model-serving:local" per D-12
    - smoke() orchestrates: build → run detached → poll /health/ready → POST /predict → assert image size &lt; 2_000_000_000 bytes → docker exec id -u == 1000 → cleanup container
  </behavior>
  <action>
    Create scripts/__init__.py (empty package marker).

    Create scripts/docker.py per D-09/D-10/D-12:
    - Constant IMAGE = "basic-model-serving:local"
    - HOST = "127.0.0.1", PORT = 8000, BASE = f"http://{HOST}:{PORT}"
    - build(): subprocess.run(["docker", "build", "-t", IMAGE, "."], check=True)
    - run(): docker run -d --rm -p 8000:8000 --name basic-model-serving-smoke IMAGE; return container id
    - _wait_ready(timeout=120): poll urllib GET /health/ready until 200
    - _make_sample_jpeg(): generate 64x64 RGB JPEG bytes in-memory (same PIL pattern as conftest sample_jpeg_bytes)
    - _post_predict(): multipart POST /predict with generated JPEG; parse JSON; assert len(predictions)==5
    - _assert_image_size(): docker image inspect --format '{{.Size}}' must be &lt; 2_000_000_000 (D-03/D-10, CONT-01)
    - _assert_non_root(cid): docker exec cid id -u must print 1000 (D-04, CONT-02)
    - smoke(): full E2E sequence above; always stop/remove container in finally block
    - Use stdlib urllib.request for HTTP (no new pip deps on host)

    Create pyproject.toml per RESEARCH Pattern 2:
    - [project] name = "basic-model-serving", version = "0.1.0", requires-python = ">=3.12"
    - [project.scripts] docker-build = "scripts.docker:build", docker-run = "scripts.docker:run", docker-smoke = "scripts.docker:smoke"
    - [build-system] requires = ["hatchling"], build-backend = "hatchling.build"
    - [tool.hatch.build.targets.wheel] packages = ["scripts"]

    Create tests/test_docker_smoke.py:
    - Register pytest marker docker in pyproject.toml [tool.pytest.ini_options] markers
    - test_docker_smoke_e2e: skip if shutil.which("docker") is None OR docker info fails
    - Call scripts.docker.smoke(); assert no exception
    - Test MUST fail before Dockerfile exists (FileNotFoundError or docker build failure)

    Do NOT use Makefile (D-09). Do NOT add [tool.uv.scripts] — use [project.scripts] only.
  </action>
  <acceptance_criteria>
    - test file exists: tests/test_docker_smoke.py with @pytest.mark.docker
    - pyproject.toml contains docker-smoke = "scripts.docker:smoke"
    - scripts/docker.py IMAGE constant equals "basic-model-serving:local"
    - pytest tests/test_docker_smoke.py -m docker fails (RED) before Task 2 Dockerfile lands
    - grep -v '^#' scripts/docker.py | grep -c '2_000_000_000' returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f tests/test_docker_smoke.py && grep -q 'pytest.mark.docker' tests/test_docker_smoke.py && grep -q 'docker-smoke' pyproject.toml && grep -q 'basic-model-serving:local' scripts/docker.py && (pytest tests/test_docker_smoke.py -m docker -x 2>&1 | grep -qiE 'fail|error|FileNotFound|no such file' || test $? -ne 0)</automated>
  </verify>
  <done>Smoke scaffold and uv entry points exist; docker-smoke test is RED awaiting Dockerfile (D-09, D-10 scaffold, D-12 tag).</done>
</task>

<task type="auto">
  <name>Task 2: Multi-stage Dockerfile with CPU torch, baked weights, non-root runtime</name>
  <files>Dockerfile, .dockerignore</files>
  <read_first>
    - .planning/phases/02-containerization/02-CONTEXT.md — D-01, D-02, D-03, D-08, D-11
    - .planning/phases/02-containerization/02-RESEARCH.md — Pattern 1 two-stage Dockerfile, Pitfalls 1-4
    - requirements.txt — two-step CPU torch install comment and pins
    - app/models/resnet.py — ResNetClassifier build-time download trigger
    - app/main.py — uvicorn app.main:app entry, single worker comment
  </read_first>
  <action>
    Create .dockerignore per D-11:
    - Exclude: .git, .venv, __pycache__, *.pyc, .planning/, .env, .pytest_cache, .ruff_cache, .cursor/
    - Do NOT exclude tests/

    Create Dockerfile at repo root — two stages on python:3.12-slim (NOT Alpine) per carried-forward decision:

    **Builder stage (AS builder):**
    - WORKDIR /app
    - COPY requirements.txt .
    - Separate RUN for CPU torch (CRITICAL — Pitfall 3): pip install --no-cache-dir torch==2.12.1 torchvision==0.27.1 --index-url https://download.pytorch.org/whl/cpu
    - RUN pip install --no-cache-dir -r requirements.txt
    - COPY app/ ./app/
    - ENV TORCH_HOME=/app/.cache/torch per D-02
    - RUN python -c "from app.models.resnet import ResNetClassifier; ResNetClassifier()" per D-01 (bake IMAGENET1K_V2 weights)

    **Runtime stage (AS runtime):**
    - WORKDIR /app
    - RUN useradd --create-home --uid 1000 appuser per CONT-02
    - COPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages
    - COPY --from=builder /usr/local/bin /usr/local/bin
    - COPY --from=builder /app/.cache/torch /app/.cache/torch per D-01/D-02
    - COPY app/ ./app/
    - ENV TORCH_HOME=/app/.cache/torch PYTHONUNBUFFERED=1 PYTHONDONTWRITEBYTECODE=1
    - RUN mkdir -p /app/.cache/torch && chown -R appuser:appuser /app per D-02 (before USER switch)
    - USER appuser
    - EXPOSE 8000
    - HEALTHCHECK --interval=30s --timeout=5s --start-period=45s --retries=3 CMD python -c "import urllib.request; r=urllib.request.urlopen('http://127.0.0.1:8000/health/ready', timeout=3); exit(0 if r.status==200 else 1)"
    - CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"] per D-08 (single worker, no --workers, no UVICORN_* env vars)

    Do NOT expose UVICORN_HOST, UVICORN_PORT, UVICORN_WORKERS (D-08).
    Do NOT use --extra-index-url for torch (Pitfall 3).
    Do NOT use main:app — must be app.main:app (Pitfall 4).
    Do NOT download weights at runtime — builder only (D-01).
  </action>
  <acceptance_criteria>
    - docker build -t basic-model-serving:local . exits 0
    - docker image inspect basic-model-serving:local --format '{{.Size}}' prints integer &lt; 2000000000
    - docker run --rm basic-model-serving:local pip show torch 2>/dev/null | grep -q '+cpu' OR Version line lacks cu124/cu121
    - docker run --rm basic-model-serving:local id -u prints 1000
    - Container serves GET /health/ready 200 and POST /predict 200 with 5 predictions (manual or via scripts/docker.py smoke partial)
    - grep -v '^#' Dockerfile | grep -c 'app.main:app' returns at least 1
    - grep -v '^#' Dockerfile | grep -c 'USER appuser' returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && docker build -t basic-model-serving:local . && python3 -c "import subprocess; s=int(subprocess.check_output(['docker','image','inspect','basic-model-serving:local','--format','{{.Size}}'],text=True)); assert s&lt;2_000_000_000,f'{s} bytes'" && docker run --rm basic-model-serving:local id -u | grep -q '^1000$' && uv run docker-smoke</automated>
  </verify>
  <done>CONT-01 and CONT-02 satisfied: multi-stage image &lt;2GB, CPU-only torch, non-root uid 1000, baked weights at TORCH_HOME, HEALTHCHECK on /health/ready (D-01, D-02, D-03, D-08, D-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → docker build | Build context may contain secrets if .dockerignore incomplete |
| Builder stage → network | PyTorch weight download during build (expected, one-time) |
| Container runtime → client | Same API surface as Phase 1; container does not weaken SSRF/upload guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Dockerfile / image layers | mitigate | .dockerignore excludes .env per D-11; no COPY of secrets; config via runtime env only (CONT-03 in Plan 02) |
| T-02-02 | Elevation of Privilege | Container process | mitigate | USER appuser uid 1000 before CMD per CONT-02; chown TORCH_HOME before USER switch |
| T-02-03 | Denial of Service | Image size | mitigate | CPU --index-url dedicated RUN before requirements.txt; smoke asserts size &lt; 2GB |
| T-02-04 | Tampering | Model cache at runtime | mitigate | Weights baked in builder (D-01); TORCH_HOME owned by appuser read-only usage |
| T-02-SC | Tampering | pip install in Dockerfile | mitigate | RESEARCH.md Package Legitimacy Audit covers Phase 1 pins only; no new pip packages introduced |

</threat_model>

<verification>
- docker build -t basic-model-serving:local . succeeds
- Image size &lt; 2GB via docker image inspect
- docker run -d -p 8000:8000 basic-model-serving:local then curl /health/ready and POST /predict
- docker exec confirms uid 1000
- pip show torch confirms CPU build inside container
</verification>

<success_criteria>
- CONT-01: Multi-stage Dockerfile, CPU-only PyTorch, image &lt;2GB
- CONT-02: Non-root appuser uid 1000, no permission errors on startup or inference
- D-01, D-02, D-03, D-08, D-11, D-12 implemented
- D-09 scaffold: pyproject.toml + scripts/docker.py entry points exist
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `Dockerfile` | 02-01 | Multi-stage CPU-only image with baked weights and non-root runtime |
| `.dockerignore` | 02-01 | Excludes dev artifacts and secrets from build context (D-11) |
| `scripts/docker.py` | 02-01 (scaffold), 02-02 (complete smoke) | Host-side build/run/smoke orchestration (D-09, D-10) |
| `pyproject.toml` | 02-01 | uv run docker-build \| docker-run \| docker-smoke entry points (D-09) |
| `tests/test_docker_smoke.py` | 02-01 | Automated E2E gate marked @pytest.mark.docker |
| `.env.example` | 02-02 | Documents four env vars with defaults (D-07, CONT-03) |
| `README.md` (Docker section) | 02-02 | Cross-references .env.example and uv run workflow (D-07) |

<output>
Create .planning/phases/02-containerization/02-01-SUMMARY.md when done
</output>
