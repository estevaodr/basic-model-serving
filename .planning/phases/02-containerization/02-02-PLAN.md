---
phase: 02-containerization
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/docker.py
  - pyproject.toml
  - .env.example
  - README.md
  - tests/test_docker_smoke.py
  - tests/test_docker_config.py
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
user_setup: []

must_haves:
  truths:
    - "Developer can run uv run docker-smoke for one-command full E2E verification"
    - "All four application settings are configurable via environment variables without code changes"
    - ".env.example documents TORCH_NUM_THREADS, MAX_UPLOAD_BYTES, URL_TIMEOUT, LOG_LEVEL with defaults"
    - "Smoke verifies image &lt;2GB, non-root uid 1000, /health/ready, and POST /predict with 5 predictions"
    - "docker run -e TORCH_NUM_THREADS=4 applies override via existing pydantic-settings (D-05, D-06)"
  artifacts:
    - path: .env.example
      provides: Documented env var defaults for CONT-03
      contains: "TORCH_NUM_THREADS"
    - path: scripts/docker.py
      provides: Complete build/run/smoke with assertions per D-10
      contains: "def smoke"
    - path: README.md
      provides: Docker workflow section cross-referencing .env.example
      contains: "docker-smoke"
    - path: tests/test_docker_smoke.py
      provides: Passing automated docker E2E when Docker available
      contains: "test_docker_smoke_e2e"
  key_links:
    - from: scripts/docker.py
      to: Dockerfile image basic-model-serving:local
      via: docker build -t basic-model-serving:local
      pattern: "basic-model-serving:local"
    - from: docker run -e
      to: app/core/config.py
      via: pydantic-settings env auto-mapping per D-06
      pattern: "TORCH_NUM_THREADS"
    - from: README.md
      to: .env.example
      via: documentation cross-reference per D-07
      pattern: ".env.example"
---

## Phase Goal

**As a** developer, **I want to** run one-command container smoke tests and configure the service via environment variables, **so that** I can verify and tune the containerized API without editing code or Dockerfiles.

<objective>
Complete the container developer workflow: `.env.example` for CONT-03, README Docker section (D-07), hardened `uv run docker-smoke` with full E2E assertions (D-04, D-10), and a GREEN automated test.

Purpose: Second vertical slice — after this plan, `uv run docker-smoke` is the single acceptance command for Phase 2.
Output: .env.example, README Docker section, complete scripts/docker.py, passing tests/test_docker_smoke.py.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-containerization/02-CONTEXT.md
@.planning/phases/02-containerization/02-RESEARCH.md
@app/core/config.py
@.planning/phases/02-containerization/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Environment configuration surface — .env.example and README Docker section</name>
  <files>.env.example, README.md</files>
  <read_first>
    - .planning/phases/02-containerization/02-CONTEXT.md — D-05, D-06, D-07 locked decisions
    - app/core/config.py — four Settings fields and their defaults
    - .planning/phases/02-containerization/02-RESEARCH.md — .env.example template
  </read_first>
  <action>
    Create .env.example at repo root per D-07 with exactly four vars matching app/core/config.py defaults (D-05 — no fifth var):
    TORCH_NUM_THREADS=2
    MAX_UPLOAD_BYTES=1048576
    URL_TIMEOUT=5.0
    LOG_LEVEL=INFO
    Include brief comment header: copy to .env for local dev; container uses docker run -e or compose env in Phase 3.

    Update README.md (currently stub) with a **Docker** section per D-07:
    - Prerequisites: Docker Engine, uv (optional but recommended)
    - Build: docker build -t basic-model-serving:local . OR uv run docker-build
    - Run: docker run --rm -p 8000:8000 basic-model-serving:local OR uv run docker-run
    - Config: reference .env.example; show example docker run -e TORCH_NUM_THREADS=4 -e LOG_LEVEL=DEBUG
    - Smoke: uv run docker-smoke (full E2E per D-10)
    - Note: Uvicorn host/port/workers are hardcoded in Dockerfile CMD (D-08), not env-configurable

    Do NOT add UVICORN_* vars to .env.example (D-08).
    Do NOT add new Settings fields to app/core/config.py unless Dockerfile strictly requires (D-05).
    Do NOT create docker-compose.yml (Phase 3 / CONT-04).
  </action>
  <acceptance_criteria>
    - .env.example exists with exactly 4 non-comment lines for TORCH_NUM_THREADS, MAX_UPLOAD_BYTES, URL_TIMEOUT, LOG_LEVEL
    - grep -v '^#' .env.example | grep -vc 'UVICORN' returns 0 (no UVICORN vars)
    - README.md contains "docker-smoke" and ".env.example"
    - docker run --rm -e LOG_LEVEL=DEBUG basic-model-serving:local python -c "from app.core.config import settings; assert settings.log_level=='DEBUG'" succeeds after image built
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .env.example && grep -q 'TORCH_NUM_THREADS=2' .env.example && grep -q 'MAX_UPLOAD_BYTES=1048576' .env.example && grep -q 'docker-smoke' README.md && grep -q '.env.example' README.md && docker run --rm -e LOG_LEVEL=DEBUG basic-model-serving:local python -c "from app.core.config import settings; assert settings.log_level=='DEBUG'" && docker run --rm -e TORCH_NUM_THREADS=4 basic-model-serving:local python -c "from app.core.config import settings; assert settings.torch_num_threads==4"</automated>
  </verify>
  <done>CONT-03 documentation complete: four env vars documented in .env.example; README cross-references config (D-05, D-06, D-07).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete uv run docker-smoke and GREEN E2E test</name>
  <files>scripts/docker.py, pyproject.toml, tests/test_docker_smoke.py, tests/test_docker_config.py</files>
  <read_first>
    - .planning/phases/02-containerization/02-CONTEXT.md — D-04, D-09, D-10, D-12
    - .planning/phases/02-containerization/02-RESEARCH.md — smoke JPEG generation, image size assertion, non-root check
    - scripts/docker.py — scaffold from Plan 02-01 Task 1
    - tests/test_docker_smoke.py — RED test from Plan 02-01
  </read_first>
  <behavior>
    - uv run docker-smoke exits 0 when Docker daemon available
    - smoke() prints clear step labels; fails fast with assertion messages on size, uid, health, or predict
    - test_docker_smoke_e2e passes (GREEN) when Docker available; skips gracefully when Docker absent
  </behavior>
  <action>
    Harden scripts/docker.py smoke() per D-04/D-10:
    1. build() — docker build -t basic-model-serving:local .
    2. _assert_image_size() — Size &lt; 2_000_000_000 bytes (CONT-01, D-03)
    3. Start container: docker run -d --rm -p 8000:8000 --name basic-model-serving-smoke basic-model-serving:local
    4. _assert_non_root(cid) — docker exec id -u == "1000\n" (D-04, CONT-02)
    5. _wait_ready() — poll GET /health/ready until HTTP 200 (timeout 120s)
    6. _post_predict() — POST multipart /predict; assert response JSON has predictions list length 5 with label and confidence keys
    7. Optional: verify pip show torch +cpu inside running container via docker exec
    8. finally: docker stop basic-model-serving-smoke (ignore errors if already stopped)

    Improve run() for standalone use: same port mapping, print URL hint on success.

    Ensure pyproject.toml [tool.pytest.ini_options] markers includes: docker: needs Docker daemon for container E2E

    Update tests/test_docker_smoke.py:
    - test_docker_smoke_e2e calls scripts.docker.smoke() and asserts return/ no exception
    - Skip with pytest.skip if docker CLI missing or docker info fails

    Create tests/test_docker_config.py (required, no Docker needed):
    - Verify .env.example keys match Settings model field env names (torch_num_threads → TORCH_NUM_THREADS) per D-06
    - Add tests/test_docker_config.py to files_modified in plan frontmatter

    Verify uv run docker-build, uv run docker-run, uv run docker-smoke all resolve (D-09).
  </action>
  <acceptance_criteria>
    - uv run docker-smoke exits 0 on host with Docker (full E2E per D-10)
    - pytest tests/test_docker_smoke.py -m docker passes when Docker available
    - smoke path asserts image size &lt; 2GB and uid 1000
    - POST /predict returns exactly 5 predictions in smoke output
    - grep -v '^#' scripts/docker.py | grep -c 'health/ready' returns at least 1
    - grep -v '^#' scripts/docker.py | grep -c 'predictions' returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && uv run docker-smoke && pytest tests/test_docker_smoke.py -m docker -x -q && pytest tests/test_docker_config.py -x -q</automated>
  </verify>
  <done>Full E2E smoke as non-root passes via uv run docker-smoke (D-04, D-09, D-10, D-12); automated test GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host smoke script → Docker daemon | Subprocess invokes docker CLI with fixed image name; no user-controlled shell injection |
| .env.example → developer | Example file must not contain real secrets |
| docker run -e → container | Env vars tune app behavior; must not expose bind settings (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | .env.example | mitigate | Placeholder defaults only; .gitignore already excludes .env; document copy-to-.env pattern |
| T-02-06 | Tampering | smoke script subprocess | mitigate | Use subprocess.run with list args (no shell=True); fixed IMAGE constant |
| T-02-07 | Denial of Service | smoke poll loop | mitigate | _wait_ready timeout 120s max; container stopped in finally block |
| T-02-08 | Spoofing | smoke HTTP client | accept | localhost-only requests to container under test; no external URLs in smoke path |

</threat_model>

<verification>
- uv run docker-smoke completes with exit code 0
- pytest tests/test_docker_smoke.py -m docker passes (or skips if no Docker)
- docker run -e TORCH_NUM_THREADS=4 applies inside container
- README Docker section accurate and references .env.example
</verification>

<success_criteria>
- CONT-03: Configuration via environment variables documented and verified with docker run -e
- D-04: Full smoke as non-root user with health + predict
- D-05, D-06, D-07, D-09, D-10, D-12 complete
- Phase 2 success criteria from ROADMAP all TRUE
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `Dockerfile` | 02-01 | Multi-stage CPU-only image with baked weights and non-root runtime |
| `.dockerignore` | 02-01 | Excludes dev artifacts and secrets from build context (D-11) |
| `scripts/docker.py` | 02-01 + 02-02 | Complete build/run/smoke orchestration (D-09, D-10) |
| `pyproject.toml` | 02-01 + 02-02 | uv run entry points and pytest docker marker |
| `tests/test_docker_smoke.py` | 02-01 + 02-02 | GREEN automated E2E when Docker available |
| `.env.example` | 02-02 | Four env vars with defaults (D-07, CONT-03) |
| `README.md` | 02-02 | Docker workflow section with .env.example cross-reference (D-07) |

<output>
Create .planning/phases/02-containerization/02-02-SUMMARY.md when done
</output>
