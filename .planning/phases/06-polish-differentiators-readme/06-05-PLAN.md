---
phase: 06-polish-differentiators-readme
plan: 05
type: execute
wave: 2
depends_on:
  - "06-04"
files_modified:
  - README.md
  - .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md
autonomous: true
gap_closure: true
requirements:
  - PERF-01
  - PERF-03
user_setup:
  - service: hey
    why: "Re-benchmark after compose tuning (06-04)"
    env_vars: []
    dashboard_config:
      - task: "Ensure hey installed before benchmark"
        location: "go install github.com/rakyll/hey@latest"

must_haves:
  truths:
    - "Post-tuning benchmark run captured with p50/p95/p99, RPS, error rate, raw and normalized peak CPU"
    - "README Performance Results table updated with post-tuning numbers, run date, host specs, and tuning config note (D-09)"
    - "PERF-01 and PERF-03 gaps closed either by meeting SLO on tuned host OR documented VERIFICATION.md overrides with D-09 rationale"
  artifacts:
    - path: README.md
      provides: Updated Performance Results with post-tuning evidence
      contains: "Performance Results"
    - path: .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md
      provides: Gap closure overrides when SLO still missed after tuning
      contains: "overrides"
  key_links:
    - from: README.md
      to: scripts/load-test.sh
      via: Reproduce section and updated metrics from 06-05 benchmark
      pattern: "load-test.sh"
    - from: 06-VERIFICATION.md
      to: README.md
      via: override reasons reference D-09 honest publishing
      pattern: "D-09"
---

## Phase Goal

**As a** portfolio reviewer, **I want to** reproduce published load-test evidence against the compose stack, **so that** I can verify the <100ms p95 claim with concrete numbers rather than trust alone.

<objective>
Re-run compose benchmark after 06-04 tuning, update README Performance Results (D-06, D-08, D-09), and close PERF-01/PERF-03 verification gaps — either by meeting SLO targets on tuned stack or by adding documented VERIFICATION.md overrides accepting environment-specific miss with published evidence per D-09.

Purpose: Gap closure requires tuning attempt BEFORE override; honest numbers either way.
Output: 06-05-SUMMARY benchmark log, updated README table, VERIFICATION.md overrides if needed.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md
@.planning/phases/06-polish-differentiators-readme/06-CONTEXT.md
@.planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md
@.planning/phases/06-polish-differentiators-readme/06-04-SUMMARY.md
@scripts/load-test.sh
@docker-compose.yml
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run tuned compose benchmark and capture evidence</name>
  <files>scripts/load-test.sh</files>
  <read_first>
    - .planning/phases/06-polish-differentiators-readme/06-04-SUMMARY.md — tuning applied (CPU limit, TORCH_NUM_THREADS, script fixes)
    - docker-compose.yml — post-06-04 app resources
    - scripts/load-test.sh — dual CPU output format
    - .planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md — baseline metrics for comparison (p95 798ms, peak 740.17%)
  </read_first>
  <action>
    Execute post-tuning benchmark per D-01 and gap closure attempt-first policy:
    1. Rebuild image if needed: uv run docker-build
    2. docker compose down && docker compose up -d — pick up 06-04 CPU limit and TORCH_NUM_THREADS
    3. Wait for curl -sf http://localhost:8000/health/ready
    4. Run ./scripts/load-test.sh; capture full stdout to 06-05-SUMMARY.md benchmark section
    5. Parse p50/p95/p99 (ms), RPS, error rate from hey output; record raw peak CPU and normalized peak CPU from script output
    6. Record host specs (nproc, free -h, uname, docker version), run date UTC, and tuning config (cpus limit, TORCH_NUM_THREADS value)
    7. Compare to 06-01 baseline: note whether p95 improved and whether normalized CPU &lt; 70%
    8. Record SLO pass/fail flags for PERF-01 (p95 &lt; 100ms) and PERF-03 (normalized peak CPU &lt; 70%)

    Do NOT fabricate metrics. Do NOT skip benchmark if stack available. Do NOT target minikube.
  </action>
  <acceptance_criteria>
    - 06-05-SUMMARY.md contains complete benchmark log with all metrics and tuning config
    - hey -c 10 run completes with predominantly HTTP 200 (PERF-02 preserved)
    - SLO pass/fail explicitly recorded for PERF-01 and PERF-03
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .planning/phases/06-polish-differentiators-readme/06-05-SUMMARY.md && grep -q 'p95' .planning/phases/06-polish-differentiators-readme/06-05-SUMMARY.md && grep -q 'normalized' .planning/phases/06-polish-differentiators-readme/06-05-SUMMARY.md</automated>
  </verify>
  <done>Post-tuning benchmark evidence captured; SLO outcomes documented for README and verification closure.</done>
</task>

<task type="auto">
  <name>Task 2: Update README and close VERIFICATION gaps (SLO or D-09 override)</name>
  <files>README.md, .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md</files>
  <read_first>
    - README.md ## Performance Results section — current table and SLO note
    - .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md — gaps block and override template
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-06, D-08, D-09
    - 06-05-SUMMARY benchmark metrics from Task 1
  </read_first>
  <action>
    Update README.md Performance Results per D-06, D-08, D-09:
    - Replace table values with post-tuning metrics from Task 1 (p50, p95, p99, RPS, error rate)
    - Add rows or footnote for Peak CPU raw and Peak CPU normalized (host cores)
    - Update run date and host specs; note tuning applied (compose cpus: 2.0, TORCH_NUM_THREADS=2)
    - Revise SLO note: if p95 &lt; 100ms celebrate; if still exceeded, keep honest D-09 note explaining sync handler + ResNet-50 + concurrent load, cite tuning attempted in 06-04/06-05
    - Update CPU footnote to explain raw vs normalized docker stats (PERF-03 evaluation uses normalized %)

    Close VERIFICATION.md gaps per attempt-first then override policy:
    - If PERF-01 satisfied (p95 &lt; 100ms): update gap status in frontmatter or add resolution note; set overrides_applied only if used
    - If PERF-01 still failed after tuning: add overrides entry per VERIFICATION template with D-09 rationale (honest publishing, tooling reproducible, tuning attempted with TORCH_NUM_THREADS=2 and 2-CPU limit)
    - If PERF-03 satisfied (normalized peak CPU &lt; 70%): mark gap closed
    - If PERF-03 still failed: add override with normalization rationale (multi-core docker stats, documented host core count, portfolio environment-specific target)
    - Update frontmatter: status to gaps_closed or partial per outcomes; increment overrides_applied count

    Do NOT remove existing README sections below Performance Results (D-17). Do NOT claim SLO pass without measured evidence.
  </action>
  <acceptance_criteria>
    - README Performance Results table reflects 06-05 benchmark numbers
    - README documents tuning config and raw vs normalized CPU
    - 06-VERIFICATION.md gaps resolved via SLO pass OR overrides with D-09 citations
    - grep README for Performance Results still passes existing doc structure
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -q 'Performance Results' README.md && grep -q 'TORCH_NUM_THREADS' README.md && (grep -q 'overrides:' .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md || grep -q 'gaps_closed' .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md)</automated>
  </verify>
  <done>PERF-01 and PERF-03 gaps closed in verification artifact; README publishes honest post-tuning evidence per D-09.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Published README metrics → reviewer trust | Performance claims must match captured benchmark log |
| VERIFICATION overrides → requirement status | Overrides must cite D-09 and tuning attempt evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Repudiation | README performance table | mitigate | Metrics sourced from 06-05-SUMMARY raw hey output |
| T-06-05-02 | Tampering | VERIFICATION override without tuning | mitigate | 06-04/06-05 must run before override; reference SUMMARY |
| T-06-05-SC | Tampering | hey binary | accept | Same official source as 06-01 user_setup |

</threat_model>

<verification>
- 06-05-SUMMARY.md benchmark log complete
- README Performance Results updated with post-tuning numbers
- 06-VERIFICATION.md reflects closed gaps or documented D-09 overrides
</verification>

<success_criteria>
- PERF-01: p95 measured post-tuning; gap closed by SLO pass OR D-09 override with evidence
- PERF-03: normalized CPU measured post-tuning; gap closed by SLO pass OR documented normalization override
- D-06, D-08, D-09 satisfied in updated README
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `06-05-SUMMARY.md` | 06-05 | Post-tuning benchmark evidence log |
| `README.md` | 06-05 | Updated Performance Results table and SLO note |
| `06-VERIFICATION.md` | 06-05 | Gap closure status and optional D-09 overrides |

<output>
Create `.planning/phases/06-polish-differentiators-readme/06-05-SUMMARY.md` when done
</output>
