---
phase: 06-polish-differentiators-readme
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - monitoring/grafana/provisioning/alerting/latency.yml
  - tests/test_grafana_alerting.py
autonomous: true
requirements:
  - DOC-06
user_setup: []

must_haves:
  truths:
    - "Grafana Alerting shows High Latency rule provisioned on compose startup alongside Service Down"
    - "High Latency evaluates p95 /predict latency > 100ms for 2 minutes using dashboard-matching PromQL"
    - "Alert YAML has no email/Slack/webhook contact points (D-14)"
    - "Static contract tests prevent alert/dashboard PromQL drift"
  artifacts:
    - path: monitoring/grafana/provisioning/alerting/latency.yml
      provides: Provisioned High Latency unified alert rule
      contains: "High Latency"
    - path: tests/test_grafana_alerting.py
      provides: Latency alert contract tests
      contains: "test_latency_alert"
  key_links:
    - from: monitoring/grafana/provisioning/alerting/latency.yml
      to: request_duration_bucket{path="/predict"}
      via: refId A PromQL histogram_quantile
      pattern: 'request_duration_bucket{path="/predict"}'
    - from: monitoring/grafana/provisioning/alerting/latency.yml
      to: monitoring/grafana/dashboards/model-serving-overview.json
      via: identical p95 expression
      pattern: "histogram_quantile(0.95"
---

## Phase Goal

**As a** portfolio reviewer, **I want to** see a provisioned High Latency SLO alert distinct from Service Down, **so that** I can demo latency-based observability without conflating downtime signals.

<objective>
Deliver the SLO alerting vertical slice at the config layer: provision `latency.yml` High Latency rule (p95 > 100ms for 2m) in compose Grafana, extend static contract tests — mirroring Phase 3 downtime pattern (D-10–D-12, D-14). Runtime demo steps and human verification land in 06-03.

Purpose: DOC-06 provisioning foundation; RESEARCH Pitfall 3 (D-13 stop-app vs sustained load) resolved at demo time in 06-03 checkpoint.
Output: `latency.yml`, extended `tests/test_grafana_alerting.py`.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-differentiators-readme/06-CONTEXT.md
@.planning/phases/06-polish-differentiators-readme/06-RESEARCH.md
@.planning/phases/06-polish-differentiators-readme/06-PATTERNS.md
@monitoring/grafana/provisioning/alerting/downtime.yml
@monitoring/grafana/dashboards/model-serving-overview.json
@app/metrics/prometheus.py
@tests/test_grafana_alerting.py

<interfaces>
High Latency alert contract (D-10–D-12, D-14, RESEARCH Pattern 4):
- File: monitoring/grafana/provisioning/alerting/latency.yml (folder name alerting not alerts)
- Group: model-serving-latency, folder Model Serving, interval 10s
- Rule uid: high-latency, title: High Latency
- refId A expr: histogram_quantile(0.95, sum by (le) (rate(request_duration_bucket{path="/predict"}[5m]))) * 1000
- refId B threshold: gt 100 (milliseconds), for: 2m
- noDataState: OK, execErrState: Error
- labels: severity: warning, team: model-serving
- annotations: summary mentions p95 exceeds 100ms SLO
- No contact points (FORBIDDEN_CONTACT_TERMS in tests)
- Separate from Service Down in downtime.yml (D-10)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: High Latency alert contract tests and provisioning YAML</name>
  <files>monitoring/grafana/provisioning/alerting/latency.yml, tests/test_grafana_alerting.py</files>
  <read_first>
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-10 through D-12, D-14
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — Pattern 4 latency YAML skeleton, Pitfall 5 noDataState
    - .planning/phases/06-polish-differentiators-readme/06-PATTERNS.md — downtime.yml structural analog, dashboard p95 PromQL
    - monitoring/grafana/provisioning/alerting/downtime.yml — full two-step query model
    - monitoring/grafana/dashboards/model-serving-overview.json lines 180–191 — p95 panel expr
    - tests/test_grafana_alerting.py — existing downtime contract pattern
  </read_first>
  <behavior>
    - test_latency_alert_yaml_exists fails until latency.yml created
    - test_latency_alert_yaml_contract asserts High Latency title, histogram_quantile(0.95, request_duration_bucket path=/predict, 2m, threshold 100, no forbidden contact terms
    - test_latency_alert_promql_matches_dashboard asserts alert expr substring matches dashboard p95 panel
  </behavior>
  <action>
    Extend tests/test_grafana_alerting.py per 06-PATTERNS.md:
    - Add LATENCY_ALERT path constant to monitoring/grafana/provisioning/alerting/latency.yml
    - test_latency_alert_yaml_contract: assert High Latency, request_duration_bucket{path="/predict"}, histogram_quantile(0.95, 2m, 100, prometheus datasource, FORBIDDEN_CONTACT_TERMS scan (D-14)
    - test_latency_alert_has_required_annotations: assert p95 or latency and 100ms in annotations
    - test_latency_alert_rule_group_interval: assert 10s
    - test_latency_alert_promql_matches_dashboard: load model-serving-overview.json panel expr; assert alert file contains matching histogram_quantile substring

    Create monitoring/grafana/provisioning/alerting/latency.yml mirroring downtime.yml structure (D-10, D-11, D-12):
    - Copy two-step refId A (Prometheus instant) + refId B (threshold) pattern from downtime.yml
    - Use exact dashboard p95 PromQL as refId A expr
    - Threshold B: type gt, params [100], for: 2m
    - noDataState: OK (Pitfall 5 idle false positives)
    - severity: warning (distinct from Service Down critical)
    - annotations per RESEARCH skeleton

    Do NOT add contact points, webhooks, or email receivers (D-14). Do NOT provision in K8s Grafana (deferred). Do NOT modify downtime.yml Service Down rule.
  </action>
  <acceptance_criteria>
    - monitoring/grafana/provisioning/alerting/latency.yml exists alongside downtime.yml
    - pytest tests/test_grafana_alerting.py passes GREEN including new latency tests
    - grep -v '^#' monitoring/grafana/provisioning/alerting/latency.yml | grep -c 'High Latency' returns at least 1
    - grep -v '^#' monitoring/grafana/provisioning/alerting/latency.yml | grep -c 'histogram_quantile' returns at least 1
    - Latency alert expr matches dashboard p95 panel expression
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_grafana_alerting.py -x && test -f monitoring/grafana/provisioning/alerting/latency.yml && grep -v '^#' monitoring/grafana/provisioning/alerting/latency.yml | grep -q 'High Latency' && grep -v '^#' monitoring/grafana/provisioning/alerting/latency.yml | grep -q 'request_duration_bucket'</automated>
  </verify>
  <done>High Latency alert provisioned as code (D-10, D-11, D-12, D-14). DOC-06 config layer complete; demo pending 06-03.</done>
</task>

<task type="auto">
  <name>Task 2: Verify alert loads in compose Grafana provisioning</name>
  <files>monitoring/grafana/provisioning/alerting/latency.yml</files>
  <read_first>
    - docker-compose.yml — grafana service volume mounts for provisioning/alerting
    - .planning/phases/03-local-dev-stack-dashboards/03-03-PLAN.md — compose alert verification pattern
  </read_first>
  <action>
    Confirm compose Grafana mounts monitoring/grafana/provisioning/alerting/ directory (existing Phase 3 pattern). If stack not running, start with docker compose up -d grafana prometheus app and wait for /health/ready.

    Restart Grafana to pick up new latency.yml: docker compose restart grafana. Wait for Grafana health (curl -sf http://localhost:3000/api/health or sleep 15s).

    Required automated check when compose is up: curl Grafana API /api/v1/provisioning/alert-rules with basic auth admin:admin — verify response includes both **High Latency** and **Service Down** rule titles. Do NOT fall back to pytest if API check fails; fix compose/Grafana provisioning instead.

    Confirm both rules appear under Model Serving folder in Alerting UI.

    Do NOT add notification channels. Do NOT edit K8s helm monitoring templates.
  </action>
  <acceptance_criteria>
    - docker compose restart grafana succeeds after adding latency.yml
    - High Latency rule visible via Grafana API or documented manual UI confirmation in SUMMARY
    - Service Down rule still provisioned (no regression)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && docker compose ps -q grafana | grep -q . && docker compose restart grafana && sleep 15 && curl -sf -u admin:admin http://localhost:3000/api/v1/provisioning/alert-rules | grep -q 'High Latency' && curl -sf -u admin:admin http://localhost:3000/api/v1/provisioning/alert-rules | grep -q 'Service Down'</automated>
  </verify>
  <done>Compose Grafana loads High Latency rule at runtime. Ready for firing demo in 06-03 human checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana provisioning files → Alerting engine | Local YAML defines alert evaluation rules |
| Prometheus histogram → Grafana query | p95 derived from request_duration_bucket metrics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information Disclosure | Alert webhook in YAML | mitigate | FORBIDDEN_CONTACT_TERMS contract test; D-14 UI-only notifications |
| T-06-05 | Tampering | PromQL drift from dashboard | mitigate | test_latency_alert_promql_matches_dashboard cross-reference |
| T-06-06 | Denial of Service | Idle false-positive alerts | mitigate | noDataState: OK and for: 2m per D-11 and RESEARCH Pitfall 5 |

</threat_model>

<verification>
- pytest tests/test_grafana_alerting.py passes all latency tests
- latency.yml and downtime.yml coexist with distinct titles and queries
- Grafana compose instance loads High Latency rule after restart
</verification>

<success_criteria>
- DOC-06: High Latency SLO alert provisioned in compose Grafana (config + runtime load)
- D-10, D-11, D-12, D-14 implemented
- Alert distinct from Service Down; p95 threshold matches 100ms SLO
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `monitoring/grafana/provisioning/alerting/latency.yml` | 06-02 | High Latency unified alert (D-10–D-12) |
| Extended `tests/test_grafana_alerting.py` | 06-02 | Latency alert contract + dashboard PromQL parity |
| Grafana load confirmation in SUMMARY | 06-02 | Runtime proof rule provisioned before demo |

<output>
Create `.planning/phases/06-polish-differentiators-readme/06-02-SUMMARY.md` when done
</output>
