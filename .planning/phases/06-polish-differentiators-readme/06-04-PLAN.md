---
phase: 06-polish-differentiators-readme
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/load-test.sh
  - tests/test_load_test_script.py
  - docker-compose.yml
autonomous: true
gap_closure: true
requirements:
  - PERF-01
  - PERF-03
user_setup: []

must_haves:
  truths:
    - "load-test.sh non-200 majority guard parses hey Total duration and status-code counts correctly"
    - "load-test.sh reports peak CPU as raw docker stats % and host-normalized % (peak / nproc) for PERF-03 evaluation"
    - "Compose app service has explicit TORCH_NUM_THREADS=2 and CPU limit aligned with Phase 1 D-04 / K8s PERF-04 pattern"
  artifacts:
    - path: scripts/load-test.sh
      provides: Fixed hey output parsing and dual CPU reporting
      contains: "Peak CPU"
    - path: tests/test_load_test_script.py
      provides: Contract tests for parsing fix and normalized CPU output
      min_lines: 45
    - path: docker-compose.yml
      provides: Benchmark-tuned app resource limits and thread env
      contains: "TORCH_NUM_THREADS"
  key_links:
    - from: scripts/load-test.sh
      to: docker-compose.yml app service
      via: docker stats on compose app container under CPU limit
      pattern: "docker compose ps -q app"
    - from: docker-compose.yml
      to: .env.example
      via: TORCH_NUM_THREADS default 2
      pattern: "TORCH_NUM_THREADS"
---

## Phase Goal

**As a** portfolio reviewer, **I want to** reproduce published load-test evidence against the compose stack, **so that** I can verify the <100ms p95 claim with concrete numbers rather than trust alone.

<objective>
Close PERF-01/PERF-03 gap prerequisites: fix load-test.sh hey output parsing bug (non-200 guard), add host-normalized CPU reporting for honest PERF-03 evaluation, and apply compose benchmark tuning (TORCH_NUM_THREADS=2 per Phase 1 D-04, CPU limit matching K8s pattern) before re-benchmark in 06-05.

Purpose: Gap closure must ATTEMPT tuning first (VERIFICATION.md + D-09); tooling must correctly measure outcomes.
Output: Fixed scripts/load-test.sh, extended contract tests, tuned docker-compose.yml app service.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md
@.planning/phases/06-polish-differentiators-readme/06-CONTEXT.md
@.planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md
@.planning/phases/06-polish-differentiators-readme/06-01-PLAN.md
@scripts/load-test.sh
@tests/test_load_test_script.py
@docker-compose.yml
@.env.example
@.helm/templates/configmap.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix hey output parsing and add normalized CPU reporting</name>
  <files>scripts/load-test.sh, tests/test_load_test_script.py</files>
  <read_first>
    - scripts/load-test.sh lines 106-121 — current non-200 guard and Total: parsing (known bug: grep '^Total:' misses indented lines; awk $2 is duration not request count)
    - .planning/phases/06-polish-differentiators-readme/06-VERIFICATION.md — PERF-03 gap (740.17% raw ≈ 93% of 8 cores)
    - .planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md — baseline benchmark metrics
    - tests/test_load_test_script.py — existing contract pattern
  </read_first>
  <behavior>
    - test_non_200_guard_parses_indented_status_lines: fixture hey output with indented Status code distribution lines; parser sums non-200 counts correctly
    - test_total_requests_from_status_distribution: total request count derived from sum of [NNN] count responses lines, not Total: duration field
    - test_normalized_cpu_output: script prints both raw peak and host-normalized peak (peak_raw / nproc * 100 or equivalent single-line normalized %)
  </behavior>
  <action>
    Fix scripts/load-test.sh non-200 majority guard per code review finding (lines 117-121):
    - Do NOT use grep -E '^Total:' with awk $2 for request count — hey Summary Total line second field is duration (e.g. 60.3667 secs), not response count
    - Compute TOTAL_REQUESTS by summing counts from Status code distribution lines matching pattern [CODE] N responses (reuse existing while-read loop over grep -E '^\s+\[[0-9]+\]' or extend it)
    - Keep NON_200_COUNT logic; compare NON_200_COUNT to TOTAL_REQUESTS/2 only when TOTAL_REQUESTS &gt; 0

    Add host-normalized CPU reporting for PERF-03 (D-07 extension per gap closure):
    - After peak raw CPU from docker stats samples, set HOST_CORES=$(nproc) with fallback to 1 if nproc unavailable
    - Compute NORMALIZED_CPU as peak_raw / HOST_CORES (one decimal place acceptable via awk or bc)
    - Print two lines after hey output: Peak CPU (app container, raw): X% and Peak CPU (normalized to host cores): Y%
    - Keep existing raw line or rename clearly so README can cite both (06-05 updates table)

    Extend tests/test_load_test_script.py:
    - Add unit tests with inline hey output fixture strings (no live hey required) validating total-request extraction and non-200 sum
    - Assert script source contains normalized CPU output pattern (e.g. nproc and normalized label)
    - Preserve existing contract assertions (-c 10, sample.jpg, docker stats, etc.)

    Do NOT change hey concurrency (-c 10) or duration (-z 60s) — PERF-02 requires 10 workers.
  </action>
  <acceptance_criteria>
    - pytest tests/test_load_test_script.py passes including new parsing tests
    - Script no longer uses awk on Total: line field 2 as request count
    - Script prints raw and normalized peak CPU after benchmark section
    - grep -v '^#' scripts/load-test.sh | grep -c 'nproc' returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && uv run pytest tests/test_load_test_script.py -x && ! grep -v '^#' scripts/load-test.sh | grep -q "grep -E '\^Total:'.*awk '{print \$2}'"</automated>
  </verify>
  <done>load-test.sh correctly parses hey status counts and reports dual CPU metrics for gap re-benchmark.</done>
</task>

<task type="auto">
  <name>Task 2: Apply compose benchmark tuning (TORCH_NUM_THREADS + CPU limit)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml — current app service (env_file .env only, no CPU limit)
    - .env.example — TORCH_NUM_THREADS=2 default (Phase 1 D-04)
    - .helm/templates/configmap.yaml and .helm/templates/deployment.yaml — K8s CPU limit 2 + TORCH_NUM_THREADS=2 (PERF-04 reference)
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-09 tuning knobs
  </read_first>
  <action>
    Tune docker-compose.yml app service for benchmark reproducibility per Phase 1 D-04 and gap closure VERIFICATION missing items:
    - Add explicit environment entry TORCH_NUM_THREADS: ${TORCH_NUM_THREADS:-2} on app service (keep env_file: .env so overrides still work)
    - Add CPU limit cpus: "2.0" on app service (Compose v2 cpus field — mirrors K8s 2-core limit; avoids unbounded 8-core saturation during hey)
    - Optionally add mem_limit: 2g if consistent with K8s deployment resources; do not remove existing healthcheck/ports/env_file

    Add brief comment above app resources explaining benchmark tuning intent (TORCH_NUM_THREADS matches CPU limit per PERF-04 pattern).

    Do NOT change prometheus/grafana services. Do NOT reduce hey -c 10 concurrency in script.
  </action>
  <acceptance_criteria>
    - docker compose config validates without error
    - app service declares TORCH_NUM_THREADS default 2 and cpus limit 2.0
    - Existing healthcheck and port 8000 mapping unchanged
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && docker compose config -q && grep -q 'TORCH_NUM_THREADS' docker-compose.yml && grep -q 'cpus:' docker-compose.yml</automated>
  </verify>
  <done>Compose stack configured for tuned re-benchmark; 06-05 can restart stack and measure post-tuning metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hey output → load-test.sh parser | Untrusted text parsed for pass/fail guard |
| docker stats → CPU metrics | Host daemon metrics drive PERF-03 evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering | hey output parsing | mitigate | Unit tests with fixture output; sum status lines not Total duration |
| T-06-04-02 | Repudiation | CPU SLO evidence | mitigate | Report raw and normalized CPU; document host core count in 06-05 |
| T-06-04-SC | Tampering | compose resource config | accept | Local dev benchmark only; no new packages |

</threat_model>

<verification>
- pytest tests/test_load_test_script.py passes
- docker compose config validates with new app limits
- load-test.sh no longer mis-parses Total: line as request count
</verification>

<success_criteria>
- Code review finding (load-test.sh lines 117-121) resolved
- PERF-03 measurement tooling reports normalized CPU % alongside raw docker stats
- Compose app tuned with TORCH_NUM_THREADS=2 and 2-CPU limit before re-benchmark
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `scripts/load-test.sh` | 06-04 | Fixed parsing + dual CPU reporting |
| `tests/test_load_test_script.py` | 06-04 | Parsing and normalization contract tests |
| `docker-compose.yml` | 06-04 | Benchmark-tuned app CPU/thread config |

<output>
Create `.planning/phases/06-polish-differentiators-readme/06-04-SUMMARY.md` when done
</output>
