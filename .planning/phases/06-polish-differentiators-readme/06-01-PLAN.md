---
phase: 06-polish-differentiators-readme
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/load-test.sh
  - tests/test_load_test_script.py
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
user_setup:
  - service: hey
    why: "HTTP load generator for compose benchmark (D-02)"
    env_vars: []
    dashboard_config:
      - task: "Install hey from official GitHub source before first benchmark"
        location: "go install github.com/rakyll/hey@latest OR distro package; verify with hey -h"

must_haves:
  truths:
    - "Reviewer can run ./scripts/load-test.sh against docker compose stack and get p50/p95/p99, RPS, error rate, and peak CPU %"
    - "hey uses 10 concurrent workers for 60 seconds against POST /predict multipart upload (D-03, D-04)"
    - "Benchmark warms up with successful curl /predict before timed hey run (D-05)"
    - "CPU peak sampled via docker stats on compose app container during hey window (D-07)"
  artifacts:
    - path: scripts/load-test.sh
      provides: Reproducible compose load benchmark wrapper
      contains: "hey"
    - path: tests/test_load_test_script.py
      provides: Static contract for load-test script
      min_lines: 25
  key_links:
    - from: scripts/load-test.sh
      to: tests/fixtures/sample.jpg
      via: multipart body file field name file
      pattern: "sample.jpg"
    - from: scripts/load-test.sh
      to: http://localhost:8000/predict
      via: hey POST target
      pattern: "localhost:8000/predict"
    - from: scripts/load-test.sh
      to: docker compose app container
      via: docker compose ps -q app for stats
      pattern: "docker compose ps -q app"
---

## Phase Goal

**As a** portfolio reviewer, **I want to** reproduce published load-test evidence against the compose stack, **so that** I can verify the <100ms p95 claim with concrete numbers rather than trust alone.

<objective>
Deliver the first MVP vertical slice: a runnable `./scripts/load-test.sh` that benchmarks the compose API with hey (10 workers, 60s, multipart `sample.jpg`), captures latency percentiles, RPS, error rate, and peak CPU — satisfying PERF-01–03 at the tooling layer. Benchmark output is captured for README publication in 06-03 (DOC-05).

Purpose: Formal concurrent proof per D-01–D-05, D-07; compose-first canonical path; honest numbers even if host exceeds SLO (D-09 delegated to 06-03 README note).
Output: `scripts/load-test.sh`, `tests/test_load_test_script.py`, benchmark run log in SUMMARY for downstream README table.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-polish-differentiators-readme/06-CONTEXT.md
@.planning/phases/06-polish-differentiators-readme/06-RESEARCH.md
@.planning/phases/06-polish-differentiators-readme/06-PATTERNS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@scripts/rollout-zero-downtime.sh
@scripts/docker.py
@tests/fixtures/sample.jpg
@docker-compose.yml
@README.md

<interfaces>
Load-test contract (D-01–D-05, D-07, RESEARCH Pattern 1–3):
- Target: docker compose stack at http://localhost:8000/predict (not minikube)
- Generator: hey -m POST -c 10 -z 60s
- Payload: pre-built multipart body from tests/fixtures/sample.jpg, field name file, boundary echoed in Content-Type header
- Warm-up: curl -sf POST /predict -F file=@tests/fixtures/sample.jpg before hey
- CPU: background docker stats loop (2s interval) on container from docker compose ps -q app; report peak %
- Prerequisites: command -v hey, compose stack healthy at /health/ready
- Script header: #!/usr/bin/env bash, set -euo pipefail (rollout-zero-downtime.sh pattern)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load-test script contract tests and implementation</name>
  <files>scripts/load-test.sh, tests/test_load_test_script.py</files>
  <read_first>
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-01 through D-05, D-07
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — Pattern 1 multipart body, Pattern 3 docker stats, Pitfall 1 hey no -F
    - .planning/phases/06-polish-differentiators-readme/06-PATTERNS.md — load-test.sh analogs from rollout-zero-downtime.sh and docker.py
    - scripts/rollout-zero-downtime.sh — bash conventions
    - scripts/docker.py lines 119–136 — multipart boundary and field name file
  </read_first>
  <behavior>
    - test_load_test_script_exists fails until scripts/load-test.sh created
    - test_load_test_script_contract asserts hey -c 10 -z 60s, sample.jpg, multipart boundary, warm-up curl, docker stats
    - test_load_test_script_prerequisites_check asserts script validates hey installed and health/ready reachable
  </behavior>
  <action>
    Create tests/test_load_test_script.py with PROJECT_ROOT path resolution (mirror tests/test_grafana_alerting.py style). Assert scripts/load-test.sh contains: hey, -c 10, -z 60s, tests/fixtures/sample.jpg, multipart/form-data, boundary=, curl warm-up to /predict, docker compose ps -q app, docker stats, set -euo pipefail.

    Create scripts/load-test.sh per D-01–D-05 and D-07:
    - Validate hey installed (command -v hey) with install hint to stderr
    - Validate compose API ready: curl -sf http://localhost:8000/health/ready
    - Build multipart body temp file: printf boundary headers + cat tests/fixtures/sample.jpg + closing boundary (RESEARCH Pattern 1)
    - Warm-up: curl -sf -X POST http://localhost:8000/predict -F file=@tests/fixtures/sample.jpg; optional jq -e '.predictions | length == 5'
    - Start background docker stats on APP_ID from docker compose ps -q app; sample every 2s to temp file
    - Run hey -m POST -c 10 -z 60s -H Content-Type with boundary -D body file http://localhost:8000/predict
    - Stop stats loop; print hey summary and peak CPU % from samples
    - Exit non-zero if hey reports non-200 majority or health check fails

    Do NOT add Python load generator (D-02). Do NOT target minikube (D-01). Do NOT use hey -d plain body (Pitfall 1).
  </action>
  <acceptance_criteria>
    - scripts/load-test.sh exists, executable, passes shellcheck-free basic structure
    - pytest tests/test_load_test_script.py passes GREEN
    - grep -v '^#' scripts/load-test.sh | grep -c '\-c 10' returns at least 1
    - grep -v '^#' scripts/load-test.sh | grep -c 'sample.jpg' returns at least 1
    - grep -v '^#' scripts/load-test.sh | grep -c 'docker stats' returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_load_test_script.py -x && test -x scripts/load-test.sh && grep -v '^#' scripts/load-test.sh | grep -q '\-c 10' && grep -v '^#' scripts/load-test.sh | grep -q 'sample.jpg'</automated>
  </verify>
  <done>Load-test script contract satisfied (D-01–D-05, D-07). Reviewer has reproducible hey command wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Execute compose benchmark and capture evidence metrics</name>
  <files>scripts/load-test.sh</files>
  <read_first>
    - docker-compose.yml — service name app, port 8000
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — Pattern 2 hey output parsing, Assumption A4/A5
    - tests/test_predict.py — warm-up pattern reference
  </read_first>
  <action>
    Run end-to-end benchmark against compose stack (D-01):
    1. Ensure image built: uv run docker-build (or document skip if already built)
    2. docker compose up -d (or verify stack already running)
    3. Wait for curl -sf http://localhost:8000/health/ready
    4. Execute ./scripts/load-test.sh; capture full stdout to plan SUMMARY
    5. Parse hey Latency distribution for p50/p95/p99 (ms), Requests/sec, status code distribution for error rate, peak CPU % from script output
    6. Record host specs (nproc, free -h summary, uname, docker version) and run date for 06-03 README table (D-08)
    7. Verify PERF-02: hey -c 10 means 10+ concurrent workers; confirm [200] responses dominate status distribution
    8. Verify PERF-03: note peak CPU %; if above 70%, still record actual value (D-09 honest publishing handled in 06-03 README note)
    9. Verify PERF-01: record p95; if above 100ms, flag in SUMMARY for honest README note with TORCH_NUM_THREADS tuning (D-09)

    Do NOT skip publishing over-SLO numbers in SUMMARY. Do NOT run benchmark against minikube.
  </action>
  <acceptance_criteria>
    - ./scripts/load-test.sh completes with compose stack up and returns hey summary output
    - Captured metrics include p50, p95, p99, RPS, error rate, peak CPU %, host specs, run date in SUMMARY
    - hey status distribution shows predominantly HTTP 200 responses (PERF-02)
    - If hey or compose unavailable, document blocker in SUMMARY; do not fabricate numbers
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && docker compose ps -q app | grep -q . && curl -sf http://localhost:8000/health/ready >/dev/null && ./scripts/load-test.sh 2>&1 | tee /tmp/phase6-load-test.log && grep -q 'Latency distribution' /tmp/phase6-load-test.log</automated>
  </verify>
  <done>PERF-01–03 measured on compose stack. Benchmark evidence captured for DOC-05 README table in 06-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Load generator → compose API | External hey client sends multipart uploads to localhost:8000 |
| docker stats → container cgroup | Host reads CPU metrics from local Docker daemon |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | hey binary source | mitigate | Document install from github.com/rakyll/hey only; verify with hey -h (RESEARCH) |
| T-06-02 | Repudiation | Performance claims | mitigate | Capture raw hey output in SUMMARY; D-09 requires publishing actual numbers in README |
| T-06-03 | Denial of Service | Local compose overload | accept | Portfolio benchmark on developer machine; 60s bounded run |
| T-06-SC | Tampering | External hey install | mitigate | user_setup documents official source; no pip/npm packages added |

</threat_model>

<verification>
- pytest tests/test_load_test_script.py passes
- ./scripts/load-test.sh runs against healthy compose stack
- SUMMARY contains parsed p50/p95/p99, RPS, error rate, peak CPU %, host specs, run date
</verification>

<success_criteria>
- PERF-01: p95 latency measured and recorded (target <100ms; honest note if exceeded per D-09)
- PERF-02: 10 concurrent hey workers with successful 200 responses
- PERF-03: Peak CPU % captured via docker stats during load
- D-01, D-02, D-03, D-04, D-05, D-07 implemented in script and benchmark run
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `scripts/load-test.sh` | 06-01 | Compose hey benchmark wrapper (D-01–D-05, D-07) |
| `tests/test_load_test_script.py` | 06-01 | Static script contract tests |
| Benchmark run log in SUMMARY | 06-01 | Raw metrics for README Performance Results table (feeds DOC-05 in 06-03) |

<output>
Create `.planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md` when done
</output>
