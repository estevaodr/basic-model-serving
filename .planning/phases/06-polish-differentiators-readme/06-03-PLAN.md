---
phase: 06-polish-differentiators-readme
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - README.md
autonomous: false
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - DOC-06
user_setup: []

must_haves:
  truths:
    - "Reviewer follows TL;DR quickstart from clone to running compose stack in under 5 minutes"
    - "README includes Mermaid architecture diagram rendering on GitHub"
    - "README publishes load-test markdown table with p50/p95/p99, RPS, error rate, CPU %, host specs, run date"
    - "README documents five design decisions and 5–8 limitations with gap+next-step format"
    - "Reviewer can demo High Latency alert firing and resolving per documented steps"
    - "Existing Docker, observability, CI/CD, and Kubernetes sections remain intact below new top content"
  artifacts:
    - path: README.md
      provides: Reviewer-ready portfolio documentation
      contains: "TL;DR"
    - path: README.md
      provides: Performance evidence table
      contains: "p95 latency"
    - path: README.md
      provides: High Latency alert demo steps
      contains: "High Latency"
  key_links:
    - from: README.md Performance Results
      to: scripts/load-test.sh
      via: documented command and 06-01 SUMMARY metrics
      pattern: "load-test.sh"
    - from: README.md Alert demo High Latency
      to: monitoring/grafana/provisioning/alerting/latency.yml
      via: numbered demo steps referencing hey load
      pattern: "High Latency"
    - from: README.md Architecture
      to: docker compose up
      via: Mermaid compose subgraph
      pattern: "docker compose"
---

## Phase Goal

**As a** portfolio reviewer, **I want to** understand, run, and evaluate the project in minutes from the README alone, **so that** performance claims, architecture, and trade-offs are credible without reading the full codebase.

<objective>
Complete the reviewer-journey vertical slice: prepend TL;DR quickstart, Mermaid architecture, Performance Results table (from 06-01 benchmark), Design Decisions, Limitations, and High Latency alert demo — while preserving existing Docker/CI/K8s sections (D-15–D-22, DOC-01–DOC-06).

Purpose: Phase 6 exit — evidence-backed documentation and demonstrable SLO alert; addresses RESEARCH Open Questions Q1 (D-13 demo mechanics) via human-verify checkpoint before locking README demo steps.
Output: Extended README.md with all DOC requirements satisfied.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
@.planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md
@.planning/phases/06-polish-differentiators-readme/06-02-SUMMARY.md
</execution_context>

<context>
@.planning/phases/06-polish-differentiators-readme/06-CONTEXT.md
@.planning/phases/06-polish-differentiators-readme/06-RESEARCH.md
@.planning/phases/06-polish-differentiators-readme/06-PATTERNS.md
@README.md
@scripts/load-test.sh
@monitoring/grafana/provisioning/alerting/latency.yml
@.planning/PROJECT.md

<interfaces>
README top-section layout (D-15–D-18, RESEARCH Pattern 5):
Insert after line 3 value prop, before ## Docker:
1. ## TL;DR Quickstart — 4–6 commands: clone, uv run docker-build, docker compose up, curl predict, open Grafana; hey prerequisite note
2. ## Architecture — mermaid flowchart TB per RESEARCH diagram (compose, CI/GHCR, minikube/werf)
3. ## Performance Results — markdown table from 06-01 SUMMARY (D-06–D-09); command ./scripts/load-test.sh; host specs + run date
4. ## Design Decisions — five subsections per D-19 (werf, CI→K8s boundary, sync def handler, ResNet-50, compose vs K8s monitoring)
5. ## Limitations — 5–8 bullets gap + next step per D-20/D-21
6. Horizontal rule --- before ## Docker

High Latency demo (D-13, RESEARCH Open Questions Q1 RESOLVED):
- Add ### Alert demo (High Latency) near existing Service Down demo in Local observability stack section
- Canonical path (Path B, D-13): hey load → docker compose stop app mid-run → High Latency Firing → restart API → Normal after recovery
- Honest fallback (Path A): sustained hey load until p95 > 100ms for 2m — e.g. ./scripts/load-test.sh with extended -z 3m or hey -c 20 -z 3m while API stays up — only if Path B validation shows only Service Down fires
- Human checkpoint tries Path B first, then Path A only if High Latency does not fire
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: README reviewer funnel — quickstart, architecture, decisions, limitations</name>
  <files>README.md</files>
  <read_first>
    - README.md lines 1–120 — preserve Docker and observability sections below insert point (D-17)
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-15 through D-22
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — Pattern 5 README layout, Mermaid diagram lines 174–208
    - .planning/PROJECT.md — core value, out of scope items for limitations bullets
    - .planning/phases/04-ci-cd-pipeline/04-CONTEXT.md — CI deploy boundary for Design Decisions
    - .planning/phases/05-kubernetes-deployment-via-werf/05-CONTEXT.md — werf rationale for Design Decisions
  </read_first>
  <action>
    Prepend new sections after title and one-line value prop (line 3), before existing ## Docker (D-17, D-18):

    ## TL;DR Quickstart (D-15, DOC-02):
    - Prerequisites: Docker, uv, hey (install link github.com/rakyll/hey)
    - Steps: git clone, uv run docker-build, docker compose up, curl predict with tests/fixtures/sample.jpg, open Grafana http://localhost:3000 (admin/admin)
    - State goal: running stack in under 5 minutes
    - Link anchors to detailed sections below (Docker, Local observability stack, Kubernetes)

    ## Architecture (D-16, DOC-01):
    - Embed mermaid flowchart TB from 06-RESEARCH.md with subgraphs: reviewer (curl/hey, browser), compose (app, prometheus, grafana), cicd (ci.yml, deploy.yml, GHCR), k8s (werf, deployment, kube-prometheus-stack)
    - No binary diagram assets

    ## Design Decisions (D-19, D-22, DOC-03):
    - Dedicated subsection with exactly five decisions, each with rationale paragraph:
      1. werf for local K8s deploys
      2. CI builds/pushes only — manual werf converge boundary
      3. sync def /predict handler (event-loop tradeoff)
      4. ResNet-50 over ResNet-18
      5. compose hand-rolled monitoring vs kube-prometheus-stack in K8s
    - Cross-link existing README CI/K8s sections; do not duplicate full werf commands

    ## Limitations (D-20, D-21, DOC-04):
    - 5–8 bullets, each: **Gap** — what you'd do next
    - Topics from REQUIREMENTS out of scope: auth, HPA, cloud K8s, GPU, batching, rate limiting, Grafana default creds, CI auto-deploy
    - Honest engineer tone; reference v2 OBS items where relevant

    Add --- horizontal rule before ## Docker. Do NOT rewrite or delete existing ## Docker, ## Local observability stack, ## CI/CD, ## Kubernetes content (D-17).
  </action>
  <acceptance_criteria>
    - README contains ## TL;DR Quickstart with docker compose up and curl predict commands
    - README contains ```mermaid block with flowchart and compose subgraph
    - README contains ## Design Decisions with five named decision subsections
    - README contains ## Limitations with at least 5 bullets in gap+next-step format
    - Existing ## Docker section still present unchanged in substance below ---
    - grep -c '## Docker' README.md returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -q '## TL;DR Quickstart' README.md && grep -q '```mermaid' README.md && grep -q '## Design Decisions' README.md && grep -q '## Limitations' README.md && grep -q '## Docker' README.md && grep -q 'werf' README.md</automated>
  </verify>
  <done>DOC-01, DOC-02, DOC-03, DOC-04 satisfied. Reviewer funnel and narrative sections complete (D-15–D-18, D-19–D-22).</done>
</task>

<task type="auto">
  <name>Task 2: Performance Results table and load-test documentation</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md — captured p50/p95/p99, RPS, error rate, peak CPU, host specs, run date
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-06 through D-09
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — README performance table template
    - scripts/load-test.sh — underlying hey flags for documentation
  </read_first>
  <action>
    Add ## Performance Results section after Architecture (per D-17 order: Quickstart, Architecture, Performance, Design Decisions, Limitations — adjust insert order if Task 1 already placed sections; ensure Performance Results appears in documented order).

    Populate markdown table per D-06 with actual values from 06-01 SUMMARY:
    - p50 latency, p95 latency, p99 latency (ms)
    - RPS, error rate, peak CPU % (app container)

    Below table per D-08: Run date, Host specs (CPU/RAM/OS/Docker version), Command: ./scripts/load-test.sh (hey -c 10 -z 60s, compose stack).

    Document reproduction steps: prerequisites hey + compose up, run ./scripts/load-test.sh, reference underlying hey command with multipart body pattern from RESEARCH.

    Per D-09: if p95 exceeded 100ms on benchmark host, add honest note explaining environment-specific results and point to TORCH_NUM_THREADS and resource limit tuning — do not hide numbers.

    Link to Design Decisions sync handler and ResNet-50 choice as latency context. Do not claim CI-published artifacts (deferred).
  </action>
  <acceptance_criteria>
    - README ## Performance Results contains table with p50, p95, p99, RPS, error rate, peak CPU columns populated with real numbers from 06-01
    - README documents run date and host specs (D-08)
    - README references ./scripts/load-test.sh and hey -c 10 -z 60s
    - If p95 above 100ms, honest tuning note present (D-09)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f .planning/phases/06-polish-differentiators-readme/06-01-SUMMARY.md && grep -q '## Performance Results' README.md && grep -E '\| p50 latency \| [0-9]+' README.md && grep -E '\| p95 latency \| [0-9]+' README.md && grep -E '\| p99 latency \| [0-9]+' README.md && grep -E '\| RPS \| [0-9]' README.md && grep -E '\| (Error rate|Peak CPU)' README.md && grep -q 'load-test.sh' README.md && grep -q 'Run date' README.md</automated>
  </verify>
  <done>DOC-05 satisfied. Published load-test evidence validates <100ms claim per D-06–D-09.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: High Latency alert demo validation and README steps</name>
  <files>README.md</files>
  <read_first>
    - .planning/phases/06-polish-differentiators-readme/06-CONTEXT.md — D-13, D-14
    - .planning/phases/06-polish-differentiators-readme/06-RESEARCH.md — Pitfall 3, Open Questions Q1 (RESOLVED)
    - README.md Alert demo Service Down section — mirror numbered-step format
    - monitoring/grafana/provisioning/alerting/latency.yml — rule title and for: 2m
    - scripts/load-test.sh — sustained load option for primary demo
  </read_first>
  <action>
    Automated prep before checkpoint:
    1. Ensure compose stack running with High Latency rule loaded (from 06-02)
    2. Open Grafana → Alerting → Alert rules; confirm High Latency listed alongside Service Down (D-10)

    Human verification protocol (D-13 canonical Path B first, RESEARCH Open Questions Q1 RESOLVED):

    **Path B — D-13 stop-container mid-load (canonical per D-13):**
    1. Start hey load in background (./scripts/load-test.sh or hey -c 10 -z 120s)
    2. Mid-run: docker compose stop app
    3. Observe which alerts fire — expect **High Latency** Firing (Service Down may also fire)
    4. docker compose start app; wait for /health/ready; confirm **High Latency** returns **Normal**

    **Path A — Sustained load (fallback only if Path B does not fire High Latency):**
    1. Run ./scripts/load-test.sh OR hey -c 20 -z 3m against /predict while API stays healthy
    2. Watch Grafana High Latency rule; confirm transition to Firing within ~2–3 minutes if p95 exceeds 100ms
    3. Stop load; wait ~2 minutes; confirm returns to Normal

    After human confirms working path, add ### Alert demo (High Latency) to README Local observability stack section with numbered steps matching Path B (D-13). If only Path A fires High Latency on this hardware, document Path B steps with honest note that sustained load is the working fallback (Pitfall 3). Keep Service Down demo unchanged.

    Demo steps must include: Grafana Alerting UI navigation, hey/load-test command, expected Firing timing (~2m per D-11), restart/recovery to Normal. No Slack/email contact points (D-14).
  </action>
  <acceptance_criteria>
    - Human operator confirms High Latency alert transitions Firing then Normal using documented steps
    - README contains ### Alert demo (High Latency) with numbered steps matching validated path
    - Service Down demo section preserved unchanged
    - If D-13 stop-app path fails High Latency, README keeps Path B as canonical steps with honest sustained-load fallback note
  </acceptance_criteria>
  <what-built>High Latency alert provisioning (06-02) and prep automation; README demo section drafted pending human validation</what-built>
  <how-to-verify>
    1. docker compose up (stack healthy, Grafana admin/admin)
    2. Grafana → Alerting → confirm **High Latency** rule listed
    3. Try Path B (D-13 canonical): hey in background → `docker compose stop app` mid-run — watch for **High Latency** **Firing**
    4. Restart API (`docker compose start app`); confirm **High Latency** returns **Normal**
    5. If Path B only fires **Service Down** (not High Latency), try Path A: run `./scripts/load-test.sh` (or extended 3m run) — watch for **Firing** within ~2–3 min
    6. Record which path fired **High Latency** (not just Service Down)
    7. Type "approved" if README demo steps match what worked, or describe adjustments needed
  </how-to-verify>
  <resume-signal>Type "approved" or describe which demo path worked and any README timing tweaks needed</resume-signal>
  <verify>
    <human>Operator confirms Grafana High Latency Firing→Normal cycle and approves README demo steps</human>
  </verify>
  <done>DOC-06 satisfied end-to-end. D-13 Path B demo validated or honestly documented with sustained-load Path A fallback per RESEARCH Open Questions Q1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → reviewer actions | Untrusted reader executes documented docker/hey commands locally |
| Grafana Alerting UI | Local-only alert state; no outbound notifications (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation | Misleading performance table | mitigate | D-09 publish actual numbers; host specs and run date in README |
| T-06-08 | Information Disclosure | Grafana admin/admin in README | accept | Documented as local demo limitation in Limitations section (D-20) |
| T-06-09 | Spoofing | False SLO demo claims | mitigate | Human-verify checkpoint gates README demo steps (Assumption A3) |

</threat_model>

<verification>
- README contains all five new top sections plus Performance Results in D-17 order
- Performance table populated from 06-01 benchmark
- High Latency demo human-approved
- Existing phase tests still pass: pytest tests/test_grafana_alerting.py tests/test_load_test_script.py
</verification>

<success_criteria>
- DOC-01 through DOC-06 complete
- D-06 through D-22 implemented in README
- Phase goal: project proves performance claims and is reviewer-ready in minutes
- Service Down + High Latency demos both documented
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `README.md` (TL;DR, Architecture, Performance, Design Decisions, Limitations) | 06-03 | Reviewer funnel DOC-01–DOC-04 (D-15–D-22) |
| `README.md` (Performance Results table) | 06-03 | Load-test evidence DOC-05 (D-06–D-09) |
| `README.md` (Alert demo High Latency) | 06-03 | SLO demo steps DOC-06 (D-13, D-14) |
| Human verification record | 06-03 | Validated alert firing path per RESEARCH Open Questions Q1 |

<output>
Create `.planning/phases/06-polish-differentiators-readme/06-03-SUMMARY.md` when done
</output>
