---
phase: 01-core-inference-api
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/main.py
  - app/core/config.py
  - app/models/resnet.py
  - app/schemas/prediction.py
  - app/api/dependencies.py
  - app/api/routes/predict.py
  - app/api/routes/health.py
  - app/services/inference.py
  - tests/test_predict.py
autonomous: true
requirements:
  - API-01
  - API-03
  - API-06
  - HLTH-01
  - HLTH-02
user_setup: []

must_haves:
  truths:
    - "Developer can POST a JPEG to /predict and receive exactly 5 predictions with label strings and 0-1 confidence floats"
    - "ResNet-50 loads exactly once at startup via lifespan and is held on app.state.classifier"
    - "GET /health/live returns 200 with status alive once Uvicorn accepts connections"
    - "GET /health/ready returns 503 until model load completes then 200"
    - "/predict handler is plain def (not async def) per D-01"
    - "No semaphore or 503 saturation gate on concurrent /predict requests per D-03"
  artifacts:
    - path: app/main.py
      provides: FastAPI app with lifespan model load and router wiring
      contains: "lifespan"
    - path: app/models/resnet.py
      provides: ResNetClassifier with top-5 predict
      contains: "ResNet50_Weights"
    - path: app/api/routes/predict.py
      provides: Sync upload POST /predict
      contains: "def predict"
    - path: app/api/routes/health.py
      provides: Split liveness and readiness probes
      contains: "/health/ready"
    - path: tests/test_predict.py
      provides: Upload happy-path integration test including warm-path latency smoke
      min_lines: 15
  key_links:
    - from: app/api/routes/predict.py
      to: app/services/inference.py
      via: predict_from_bytes call
      pattern: "predict_from_bytes"
    - from: app/main.py
      to: app/models/resnet.py
      via: lifespan sets app.state.classifier
      pattern: "app.state.classifier"
    - from: app/api/routes/health.py
      to: app.state.ready
      via: readiness check
      pattern: "app.state.ready"
---

<objective>
Implement the walking skeleton: lifespan model load, split health probes, sync multipart `/predict` returning real ResNet-50 top-5 predictions, and warm-path latency smoke coverage for API-03.

Purpose: Makes plan 01-01 failing tests pass and proves the thinnest end-to-end inference path works locally.
Output: Runnable FastAPI app with upload predict, health probes, and latency smoke test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-inference-api/01-CONTEXT.md
@.planning/phases/01-core-inference-api/01-RESEARCH.md
@.planning/phases/01-core-inference-api/01-PATTERNS.md
@.planning/phases/01-core-inference-api/01-01-SUMMARY.md

<interfaces>
From plan 01-01 (must preserve test contracts):

PredictResponse: { predictions: [{ label: str, confidence: float (0-1) }] }
ErrorDetail (stub for plan 03): { error: str, message: str, request_id: str | None }
ResNetClassifier.predict(image) -> list[tuple[str, float]]  # top-5
get_classifier(request) -> ResNetClassifier  # 503 if not ready
predict_from_bytes(data: bytes, classifier) -> PredictResponse
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Walking skeleton — lifespan, health, sync upload predict</name>
  <files>app/main.py, app/core/config.py, app/models/resnet.py, app/schemas/prediction.py, app/api/dependencies.py, app/api/routes/predict.py, app/api/routes/health.py, app/services/inference.py, tests/test_predict.py</files>
  <read_first>
    - .planning/phases/01-core-inference-api/01-CONTEXT.md — D-01 through D-04 locked decisions
    - .planning/phases/01-core-inference-api/01-RESEARCH.md — Pattern 1 lifespan, Pattern 3 plain def, ResNet code example
    - .planning/phases/01-core-inference-api/01-PATTERNS.md — predict.py and resnet.py patterns
    - tests/test_predict.py — failing tests from plan 01-01
  </read_first>
  <behavior>
    - All plan 01-01 tests pass
    - predict handler is sync def (grep confirms no async def predict in predict.py)
    - Upload uses file.file.read() not await
    - test_predict_warm_path_latency_under_100ms: after one warm-up POST, second POST with sample_jpeg_bytes completes in under 100ms elapsed wall time (architectural enablement for API-03; formal p95 under 10+ concurrent load deferred to Phase 6 PERF-01)
  </behavior>
  <action>
    Implement app/core/config.py with pydantic-settings BaseSettings: torch_num_threads int default 2 (env TORCH_NUM_THREADS), max_upload_bytes int default 1048576, url_timeout float default 5.0, log_level str default INFO. Per D-04.

    Implement app/models/resnet.py ResNetClassifier per RESEARCH.md: ResNet50_Weights.IMAGENET1K_V2, eval mode, weights.transforms(), categories from weights.meta, predict() returns top-5 (label, confidence) with softmax and topk(5), decorated with torch.inference_mode(). First construction downloads weights to ~/.cache/torch (~100MB one-time); document in SUMMARY verification — no separate pre-download script.

    Implement app/schemas/prediction.py: PredictionItem (label str, confidence float ge=0 le=1), PredictResponse (predictions list). Add PredictUrlRequest stub for plan 03.

    Implement app/api/dependencies.py get_classifier: raise HTTPException 503 with detail status not_ready if app.state.ready is False; return app.state.classifier.

    Implement app/services/inference.py predict_from_bytes(data, classifier): open bytes with PIL Image.open, convert RGB, call classifier.predict, map to PredictResponse. Catch PIL.UnidentifiedImageError and raise ValueError with message for route mapping in plan 03. Do NOT increment prediction_count yet (plan 04).

    Implement app/api/routes/predict.py with ONE plain def handler per D-01:
    POST /predict accepting multipart/form-data file UploadFile = File(...).
    Use file.file.read() synchronously.
    Delegate to predict_from_bytes.
    Register router with tags Predict.

    Implement app/api/routes/health.py: GET /health/live always 200 {"status": "alive"}; GET /health/ready returns 503 {"status": "not_ready"} when app.state.ready is False else 200 {"status": "ready"}.

    Implement app/main.py:
    - lifespan async context manager: torch.set_num_threads(settings.torch_num_threads) per D-04; app.state.ready=False; load ResNetClassifier; app.state.ready=True; yield; cleanup
    - include predict and health routers
    - FastAPI title Basic Model Serving
    - Document single Uvicorn worker in module docstring per D-02: run as uvicorn app.main:app --host 0.0.0.0 --port 8000 (no --workers flag)

    Do NOT use async def for /predict per D-01. Do NOT load model per request per API-06.

    D-03 compliance: Do NOT add asyncio.Semaphore, threading.Semaphore, or any explicit concurrency cap that returns 503 when saturated. Rely on FastAPI/Uvicorn default thread pool for concurrent plain def handlers — sufficient for portfolio demo target of 10+ concurrent requests without a saturation gate.

    Add test_predict_warm_path_latency_under_100ms to tests/test_predict.py: use time.perf_counter() around a second POST /predict after warm-up; assert elapsed &lt; 0.1 seconds. Skip or xfail only if CI hardware provably slower — default is assert pass on dev host.
  </action>
  <acceptance_criteria>
    - pytest tests/test_predict.py tests/test_health.py passes
    - grep -v '^#' app/api/routes/predict.py | grep -c 'async def predict' returns 0
    - grep -c 'torch.set_num_threads' app/main.py returns at least 1
    - grep -v '^#' app/main.py app/api/routes/predict.py | grep -ci 'semaphore' returns 0 (D-03: no saturation gate)
    - Warm-path latency smoke test passes on dev hardware
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_predict.py tests/test_health.py -x -q && grep -v '^#' app/api/routes/predict.py | grep -c 'async def predict' | grep -q '^0$' && grep -v '^#' app/main.py app/api/routes/predict.py | grep -ci 'semaphore' | grep -q '^0$'</automated>
  </verify>
  <done>Upload /predict returns real top-5 ImageNet predictions; health probes work; model loads once in lifespan; all plan 01-01 tests green; API-03 warm-path smoke passes; D-03 honored (no semaphore gate).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /predict (upload) | Untrusted multipart file bytes cross into server process |
| Client → GET /health/* | Untrusted HTTP requests (low risk, no body) |
| PIL decode | Untrusted image bytes expanded in memory — decompression bomb risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | POST /predict upload | mitigate | Decode via Pillow only; catch UnidentifiedImageError (mapped in plan 03); keep MAX_IMAGE_PIXELS default |
| T-01-02 | Denial of Service | inference.py | mitigate | max_upload_bytes enforced before full read in plan 03; Pillow pixel limit retained |
| T-01-03 | Information Disclosure | error responses | mitigate | No stack traces to client; generic messages (full ErrorDetail in plan 03) |
</threat_model>

<verification>
- pytest tests/test_predict.py tests/test_health.py passes
- First-run note: torchvision downloads ResNet-50 weights to ~/.cache/torch on first lifespan (~100MB); subsequent runs use cache; optional manual warmup: python -c "from app.models.resnet import ResNetClassifier; ResNetClassifier()"
- API-03: warm-path smoke test asserts single-request latency &lt;100ms; formal p95 under concurrent load validated in Phase 6 PERF-01
- Manual smoke: uvicorn app.main:app --port 8000 then curl -F file=@sample.jpg http://localhost:8000/predict
</verification>

<success_criteria>
- API-01: multipart upload returns top-5 predictions
- API-03: warm-path latency smoke passes (architectural enablement; load-test proof in Phase 6)
- API-06: model loaded once in lifespan, app.state.classifier reused
- HLTH-01: /health/live returns 200 alive
- HLTH-02: /health/ready returns 503 until ready then 200
- D-01, D-02, D-03, D-04 implemented
</success_criteria>

<output>
Create .planning/phases/01-core-inference-api/01-02-SUMMARY.md when done
</output>
