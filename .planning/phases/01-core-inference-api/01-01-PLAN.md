---
phase: 01-core-inference-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - requirements-dev.txt
  - app/__init__.py
  - app/main.py
  - app/core/__init__.py
  - app/api/__init__.py
  - app/api/routes/__init__.py
  - app/schemas/__init__.py
  - app/services/__init__.py
  - app/models/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_predict.py
  - tests/test_health.py
autonomous: true
requirements:
  - API-01
  - HLTH-01
  - HLTH-02
user_setup: []

must_haves:
  truths:
    - "Project scaffold exists with pinned dependencies and layered app/ package layout"
    - "pytest fails for the right reason — missing routes/implementation, not import errors"
    - "TestClient can import app.main:app from stub main.py"
  artifacts:
    - path: requirements.txt
      provides: Production dependency pins including CPU torch index comment
      contains: "torch==2.12.1"
    - path: tests/conftest.py
      provides: sample_jpeg_bytes and session-scoped TestClient fixtures
      contains: "sample_jpeg_bytes"
    - path: tests/test_predict.py
      provides: Failing upload E2E test targeting five predictions
      min_lines: 10
    - path: tests/test_health.py
      provides: Failing health probe tests
      contains: "/health/live"
  key_links:
    - from: tests/conftest.py
      to: app/main.py
      via: TestClient imports app
      pattern: "from app.main import app"
---

## Phase Goal

**As a** developer integrating with the service, **I want to** POST a JPEG image and receive top-5 ImageNet predictions from a healthy API with the model loaded at startup, **so that** I have a proven inference foundation for later packaging and deployment.

<objective>
Create the greenfield project scaffold, dependency pins, test fixtures, and failing integration tests that define the walking-skeleton contract before any inference implementation.

Purpose: Establishes TDD red phase and installable project layout without exceeding per-plan file budget.
Output: requirements pins, package skeleton, conftest fixtures, failing test_predict.py and test_health.py.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-inference-api/SKELETON.md
@.planning/phases/01-core-inference-api/01-CONTEXT.md
@.planning/phases/01-core-inference-api/01-RESEARCH.md
@.planning/phases/01-core-inference-api/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md

<interfaces>
Greenfield — contracts to test against (implemented in plan 01-02):

PredictResponse: { predictions: [{ label: str, confidence: float (0-1) }] }
GET /health/live → 200 {"status": "alive"}
GET /health/ready → 200 {"status": "ready"} after lifespan
POST /predict multipart → 200 with 5 predictions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold project and failing upload E2E test</name>
  <files>requirements.txt, requirements-dev.txt, app/__init__.py, app/core/__init__.py, app/api/__init__.py, app/schemas/__init__.py, app/services/__init__.py, app/models/__init__.py, app/api/routes/__init__.py, tests/__init__.py, tests/conftest.py, tests/test_predict.py, tests/test_health.py, app/main.py</files>
  <read_first>
    - .planning/research/STACK.md — version pins and CPU torch index URL
    - .planning/phases/01-core-inference-api/01-RESEARCH.md — Installation block (lines 126-135)
    - .planning/phases/01-core-inference-api/01-PATTERNS.md — requirements.txt pattern
  </read_first>
  <behavior>
    - test_predict_upload_returns_five_predictions: POST /predict with valid JPEG bytes returns 200, JSON has predictions array of length 5, each item has label (str) and confidence (float 0-1)
    - test_health_live_returns_alive: GET /health/live returns 200 with body containing status alive
    - test_health_ready_after_startup: GET /health/ready returns 200 after TestClient lifespan completes
  </behavior>
  <action>
    Create layered package skeleton per ARCHITECTURE.md: app/{core,api/routes,schemas,services,models}/ with __init__.py files.

    Write requirements.txt with exact pins from STACK.md/RESEARCH.md: torch==2.12.1, torchvision==0.27.1 (add comment that pip install must use --index-url https://download.pytorch.org/whl/cpu), fastapi[standard]==0.139.0, pillow==12.3.0, python-multipart==0.0.32, httpx==0.28.1, pydantic-settings==2.14.2, prometheus-client==0.25.0.

    Write requirements-dev.txt: pytest==9.1.1, ruff==0.15.20.

    Create minimal app/main.py exporting FastAPI app instance (stub routers OK, no lifespan yet) so TestClient can import app.

    Create tests/conftest.py with: sample_jpeg_bytes fixture (generate small valid JPEG via Pillow in-memory, under 50KB); client fixture using TestClient(app) with scope session.

    Write failing tests in tests/test_predict.py and tests/test_health.py per behavior block. Tests MUST fail because /predict and /health routes do not exist yet.

    Install deps: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu && pip install -r requirements-dev.txt

    Note for downstream plan 01-02: first ResNetClassifier() construction triggers torchvision weight download (~100MB) to ~/.cache/torch on first lifespan run; subsequent runs use cache. No pre-download script in Phase 1 — document in 01-02-SUMMARY verification notes.
  </action>
  <acceptance_criteria>
    - requirements.txt contains torch==2.12.1 and fastapi[standard]==0.139.0
    - pytest tests/test_predict.py tests/test_health.py -x fails with route-not-found or assertion errors (not import errors)
    - TestClient imports app.main:app without error after stub main.py exists
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pip install -q -r requirements.txt --index-url https://download.pytorch.org/whl/cpu && pip install -q -r requirements-dev.txt && pytest tests/test_predict.py tests/test_health.py -x 2>&1 | head -30; test $? -ne 0</automated>
  </verify>
  <done>Failing E2E tests exist; project scaffold and dependency pins committed; pytest fails for the right reason (missing implementation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pip install | Third-party packages from PyPI / PyTorch index |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | requirements.txt pip install | mitigate | Use pinned versions from STACK.md; Package Legitimacy Audit in RESEARCH.md — all approved |
| T-01-SC | Tampering | pip package installs | mitigate | RESEARCH.md Package Legitimacy Audit completed; pins from official PyPI packages only |
</threat_model>

<verification>
- pytest tests/test_predict.py tests/test_health.py fails (red phase) with missing-route errors, not ImportError
- pip install succeeds with CPU torch index URL
</verification>

<success_criteria>
- Layered app/ package skeleton with pinned requirements.txt and requirements-dev.txt
- Failing integration tests define upload and health contract before implementation
</success_criteria>

<output>
Create .planning/phases/01-core-inference-api/01-01-SUMMARY.md when done
</output>
