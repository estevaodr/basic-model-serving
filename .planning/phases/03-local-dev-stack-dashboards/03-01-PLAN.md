---
phase: 03-local-dev-stack-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - monitoring/prometheus/prometheus.yml
  - monitoring/grafana/provisioning/datasources/prometheus.yml
  - tests/test_compose_stack.py
  - pyproject.toml
autonomous: true
requirements:
  - CONT-04
  - MON-04
  - MON-05
user_setup: []

must_haves:
  truths:
    - "Developer runs docker compose up and API, Prometheus, and Grafana all start together"
    - "Prometheus scrapes app:8000/metrics only after /health/ready passes"
    - "After curl POST /predict traffic, Prometheus shows request_count_total samples"
    - "Prometheus retains metrics for 7+ days via explicit config"
    - "Grafana has a provisioned Prometheus datasource on startup with no manual UI setup"
  artifacts:
    - path: docker-compose.yml
      provides: Three-service compose stack with health-gated scrape
      contains: "basic-model-serving:local"
    - path: monitoring/prometheus/prometheus.yml
      provides: Compose scrape config and 7d TSDB retention
      contains: "retention"
    - path: monitoring/grafana/provisioning/datasources/prometheus.yml
      provides: Auto-provisioned Grafana Prometheus datasource
      contains: "uid: prometheus"
    - path: tests/test_compose_stack.py
      provides: Automated compose stack E2E gate
      min_lines: 40
  key_links:
    - from: monitoring/prometheus/prometheus.yml
      to: app:8000/metrics
      via: static_configs targets app:8000
      pattern: "app:8000"
    - from: docker-compose.yml
      to: monitoring/prometheus/prometheus.yml
      via: bind-mount to /etc/prometheus/prometheus.yml
      pattern: "prometheus.yml"
    - from: monitoring/grafana/provisioning/datasources/prometheus.yml
      to: prometheus:9090
      via: url http://prometheus:9090
      pattern: "prometheus:9090"
---

## Phase Goal

**As a** developer, **I want to** start the full API + metrics stack with one `docker compose up`, **so that** I can iterate locally with live observability during development.

<objective>
Deliver the thinnest vertical slice of the local observability stack: compose brings up API + Prometheus + Grafana, Prometheus scrapes live Phase 1 metrics with 7d retention, and Grafana auto-wires the Prometheus datasource — no dashboard or alert yet.

Purpose: First user-visible capability — metrics flow from `/predict` traffic into Prometheus after a single compose command (D-05, D-14, CONT-04, MON-04, partial MON-05).
Output: docker-compose.yml, monitoring/prometheus/prometheus.yml, Grafana datasource provisioning, failing-then-green compose E2E test.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-local-dev-stack-dashboards/03-CONTEXT.md
@.planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md
@.planning/phases/03-local-dev-stack-dashboards/03-PATTERNS.md
@.planning/phases/02-containerization/02-02-SUMMARY.md
@app/metrics/prometheus.py
@Dockerfile
@scripts/docker.py
@.env.example

<interfaces>
From app/metrics/prometheus.py (exported Prometheus names):
- request_count → request_count_total (labels: method, path, status)
- request_duration → request_duration_bucket (labels: method, path)
- prediction_count → prediction_count_total

From scripts/docker.py:
- IMAGE = "basic-model-serving:local"
- PORT = 8000

From Dockerfile HEALTHCHECK:
- Probe: urllib GET http://127.0.0.1:8000/health/ready

Compose service DNS names (locked by scrape config):
- app (API), prometheus, grafana
- Prometheus job_name: app → alert/dashboard queries use up{job="app"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing compose stack E2E test scaffold</name>
  <files>tests/test_compose_stack.py, pyproject.toml</files>
  <read_first>
    - tests/test_docker_smoke.py — @pytest.mark.docker skip pattern and _docker_available helper
    - scripts/docker.py — IMAGE tag, _wait_ready polling, _post_predict multipart pattern
    - .planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md — compose E2E expectations and metric names
  </read_first>
  <behavior>
    - test_compose_stack_e2e marked @pytest.mark.docker; FAILS when docker-compose.yml absent
    - Test sequence: ensure image exists (call scripts.docker.build if inspect fails) → docker compose up -d → poll http://127.0.0.1:8000/health/ready → POST /predict with tests/fixtures/sample.jpg → query Prometheus http://127.0.0.1:9090/api/v1/query for up{job="app"} == 1 → query request_count_total count &gt; 0 → docker compose down -v in finally
    - Use stdlib urllib/subprocess only; no new pip dependencies
  </behavior>
  <action>
    Create tests/test_compose_stack.py with helper _docker_available() copied from test_docker_smoke.py pattern.

    Implement test_compose_stack_e2e:
    - Skip if Docker unavailable (docker info fails)
    - Prerequisite: if docker image inspect basic-model-serving:local fails, run scripts.docker.build() per D-14
    - subprocess docker compose -f docker-compose.yml up -d --wait (or manual poll with 180s timeout for app ready)
    - POST /predict using tests/fixtures/sample.jpg multipart upload to http://127.0.0.1:8000/predict
    - GET http://127.0.0.1:9090/api/v1/query?query=up{job="app"} — assert at least one result value == "1"
    - GET http://127.0.0.1:9090/api/v1/query?query=request_count_total — assert result length &gt; 0
    - finally: subprocess docker compose down -v (check=False) to clean up

    Add pytest marker compose to pyproject.toml [tool.pytest.ini_options] markers alongside existing docker marker: "compose: needs Docker daemon and docker compose stack".

    Do NOT add uv run compose-* scripts (D-16). Do NOT use build: in compose file.
  </action>
  <acceptance_criteria>
    - tests/test_compose_stack.py exists with @pytest.mark.docker or @pytest.mark.compose
    - Test references docker-compose.yml and basic-model-serving:local
    - pytest tests/test_compose_stack.py -m docker fails RED before Task 2 (missing compose file or services)
    - Test uses tests/fixtures/sample.jpg for predict traffic per D-06
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f tests/test_compose_stack.py && grep -q 'compose' tests/test_compose_stack.py && grep -q 'basic-model-serving:local' tests/test_compose_stack.py && (pytest tests/test_compose_stack.py -m docker -x 2>&1 | grep -qiE 'fail|error|FileNotFound|no such file' || test $? -ne 0)</automated>
  </verify>
  <done>Compose E2E test scaffold exists and is RED awaiting docker-compose.yml (MVP failing E2E gate).</done>
</task>

<task type="auto">
  <name>Task 2: Compose stack with Prometheus scrape and Grafana datasource</name>
  <files>docker-compose.yml, monitoring/prometheus/prometheus.yml, monitoring/grafana/provisioning/datasources/prometheus.yml</files>
  <read_first>
    - .planning/phases/03-local-dev-stack-dashboards/03-PATTERNS.md — docker-compose.yml skeleton, prometheus scrape pattern, datasource YAML
    - Dockerfile lines 41-42 — HEALTHCHECK urllib /health/ready probe to mirror in compose
    - .env.example — four app env vars for app.env_file
    - .planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md — Pattern 1/2/3, image pins prom/prometheus:v3.3.0 grafana/grafana:11.6.0
  </read_first>
  <action>
    Create monitoring/prometheus/prometheus.yml per MON-04 and RESEARCH Pattern 2:
    - global scrape_interval: 15s, evaluation_interval: 15s
    - storage.tsdb.retention.time: 7d (explicit MON-04 — not default)
    - scrape_configs job_name: app, metrics_path: /metrics, targets: ['app:8000']

    Create monitoring/grafana/provisioning/datasources/prometheus.yml per RESEARCH Pattern 3:
    - apiVersion: 1
    - datasource name Prometheus, type prometheus, uid: prometheus, access: proxy, url: http://prometheus:9090, isDefault: true, jsonData.httpMethod: POST

    Create docker-compose.yml at repo root per D-05/D-07/D-13/D-14:
    - services.app: image basic-model-serving:local (NO build:), ports 8000:8000, env_file .env, healthcheck mirroring Dockerfile urllib /health/ready probe with interval 10s timeout 5s retries 6 start_period 60s (D-11 boot grace)
    - services.prometheus: image prom/prometheus:v3.3.0, ports 9090:9090, volumes bind-mount prometheus.yml ro + named volume prometheus-data:/prometheus, command --config.file=/etc/prometheus/prometheus.yml --web.enable-lifecycle, depends_on app condition service_healthy
    - services.grafana: image grafana/grafana:11.6.0, ports 3000:3000, volumes bind-mount ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro, environment GF_SECURITY_ADMIN_USER=admin GF_SECURITY_ADMIN_PASSWORD=admin GF_USERS_DEFAULT_THEME=dark (D-08), depends_on prometheus
    - volumes.prometheus-data for TSDB persistence across restarts (MON-04)
    - No uv wrapper scripts; plain docker compose up entry (D-05, D-16)

    Do NOT add build: directive (D-14). Do NOT bind-mount API source (D-13). Do NOT expose Alertmanager. Do NOT use http_requests_total in any config.
  </action>
  <acceptance_criteria>
    - docker compose config exits 0 (valid compose file)
    - docker compose up -d starts three services; curl http://127.0.0.1:8000/health/ready returns 200 within 120s
    - Prometheus Targets UI shows job app UP at http://127.0.0.1:9090/targets
    - grep -v '^#' monitoring/prometheus/prometheus.yml | grep -c '7d' returns at least 1
    - grep -v '^#' monitoring/prometheus/prometheus.yml | grep -c 'app:8000' returns at least 1
    - Grafana login at :3000 with admin/admin succeeds; Configuration → Data sources shows Prometheus default
    - pytest tests/test_compose_stack.py -m docker passes GREEN
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && docker compose config -q && grep -v '^#' monitoring/prometheus/prometheus.yml | grep -q '7d' && grep -v '^#' monitoring/prometheus/prometheus.yml | grep -q 'app:8000' && grep -v '^#' docker-compose.yml | grep -q 'basic-model-serving:local' && grep -v '^#' docker-compose.yml | grep -qv 'build:' && pytest tests/test_compose_stack.py -m docker -x</automated>
  </verify>
  <done>CONT-04 and MON-04 satisfied; Grafana datasource provisioned on startup (partial MON-05). Developer can docker compose up and see live metrics in Prometheus (D-05, D-07, D-14, D-15).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host → Docker Compose | Developer runs compose; exposes ports 8000/9090/3000 on localhost |
| Prometheus → app:8000/metrics | Internal compose network scrape of unauthenticated metrics endpoint |
| Browser → Grafana :3000 | Admin login with default credentials (local demo only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing | Grafana admin/admin on :3000 | accept | Document local-only use in README (D-08); not exposed to internet |
| T-03-02 | Information Disclosure | Prometheus :9090 open | accept | Local demo; no admin API enabled; document port table |
| T-03-03 | Denial of Service | False downtime alert on boot | mitigate | App healthcheck start_period 60s + prometheus depends_on service_healthy (D-11) |
| T-03-04 | Tampering | Bind-mounted monitoring configs | mitigate | Mount :ro; changes via git only (D-15) |
| T-03-SC | Tampering | prom/prometheus and grafana/grafana images | mitigate | RESEARCH Package Legitimacy Audit OK; pin semver tags v3.3.0 and 11.6.0 |

</threat_model>

<verification>
- docker compose up starts app, prometheus, grafana without manual steps
- Prometheus retention explicitly 7d in config file
- After sample predict traffic, request_count_total visible in Prometheus
- Grafana datasource auto-provisioned with uid prometheus
</verification>

<success_criteria>
- CONT-04: docker-compose.yml runs API + Prometheus + Grafana together
- MON-04: Explicit 7d TSDB retention with named volume persistence
- MON-05 partial: Grafana Prometheus datasource provisioned as code
- D-05, D-07, D-08, D-13, D-14, D-15, D-16 honored
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `docker-compose.yml` | 03-01 | Three-service local stack; plain docker compose up entry (CONT-04, D-05) |
| `monitoring/prometheus/prometheus.yml` | 03-01 | Compose scrape target app:8000 + 7d retention (MON-04) |
| `monitoring/grafana/provisioning/datasources/prometheus.yml` | 03-01 | Auto-provisioned Prometheus datasource (MON-05 partial) |
| `tests/test_compose_stack.py` | 03-01 | Compose stack E2E automated gate |

<output>
Create .planning/phases/03-local-dev-stack-dashboards/03-01-SUMMARY.md when done
</output>
