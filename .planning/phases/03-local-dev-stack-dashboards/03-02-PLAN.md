---
phase: 03-local-dev-stack-dashboards
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - monitoring/grafana/dashboards/model-serving-overview.json
  - monitoring/grafana/provisioning/dashboards/dashboard.yml
  - docker-compose.yml
  - tests/test_grafana_dashboard.py
  - tests/test_compose_stack.py
autonomous: true
requirements:
  - MON-02
  - MON-05
user_setup: []

must_haves:
  truths:
    - "Reviewer logs into Grafana and lands on Model Serving Overview home dashboard"
    - "Dashboard shows 8 panels with live data after manual curl POST /predict traffic"
    - "p50 and p95 latency display as stat panels in milliseconds with SLO thresholds"
    - "4xx and 5xx errors appear as separate series using request_count status labels"
    - "Dashboard and provider are provisioned from git on compose startup — no manual import"
  artifacts:
    - path: monitoring/grafana/dashboards/model-serving-overview.json
      provides: Model Serving Overview dashboard with 8 panels
      contains: "model-serving-overview"
    - path: monitoring/grafana/provisioning/dashboards/dashboard.yml
      provides: File-based dashboard provider
      contains: "model-serving"
    - path: tests/test_grafana_dashboard.py
      provides: Static validation of dashboard contract
      min_lines: 30
  key_links:
    - from: monitoring/grafana/dashboards/model-serving-overview.json
      to: prometheus datasource uid
      via: "uid": "prometheus"
      pattern: '"uid": "prometheus"'
    - from: docker-compose.yml
      to: model-serving-overview.json
      via: GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH
      pattern: "model-serving-overview.json"
    - from: monitoring/grafana/dashboards/model-serving-overview.json
      to: request_count_total
      via: PromQL in panel targets
      pattern: "request_count_total"
---

## Phase Goal

**As a** developer, **I want to** start the full API + metrics stack with one `docker compose up`, **so that** I can iterate locally with live Grafana dashboards during development.

<objective>
Deliver the dashboard vertical slice: provisioned Model Serving Overview with 8 ops-standard panels showing live Phase 1 metrics, home-dashboard landing, and automated static contract tests.

Purpose: After this plan, reviewer opens Grafana :3000 and sees real request/latency/error/prediction data without manual dashboard setup (D-01–D-04, MON-02, MON-05 dashboard portion).
Output: Dashboard JSON, dashboard provider YAML, compose home-dashboard env, validation tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-local-dev-stack-dashboards/03-UI-SPEC.md
@.planning/phases/03-local-dev-stack-dashboards/03-PATTERNS.md
@.planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md
@app/metrics/prometheus.py
@monitoring/grafana/provisioning/datasources/prometheus.yml

<interfaces>
Dashboard metadata (03-UI-SPEC.md):
- uid: model-serving-overview
- title: Model Serving Overview
- editable: false, refresh: 10s, timezone: browser, tags: model-serving, portfolio, phase-3

Panel PromQL (use _total / _bucket suffixes):
- Request Rate: sum(rate(request_count_total[5m]))
- p50: histogram_quantile(0.50, sum by (le) (rate(request_duration_bucket{path="/predict"}[5m]))) * 1000
- p95: histogram_quantile(0.95, sum by (le) (rate(request_duration_bucket{path="/predict"}[5m]))) * 1000
- Error Rate: sum(rate(request_count_total{status=~"5.."}[5m])) / sum(rate(request_count_total[5m]))
- Throughput: rate(prediction_count_total[5m])
- 4xx: sum(rate(request_count_total{status=~"4.."}[5m]))
- 5xx: sum(rate(request_count_total{status=~"5.."}[5m]))
- Service Health: up{job="app"}
- Total Predictions: prediction_count_total

Stat panels (D-02): graphMode none. Latency thresholds: green &lt;100ms yellow 100-200 red &gt;200.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard contract validation tests</name>
  <files>tests/test_grafana_dashboard.py</files>
  <read_first>
    - .planning/phases/03-local-dev-stack-dashboards/03-UI-SPEC.md — panel inventory, grid layout, PromQL, thresholds
    - tests/test_metrics.py — metric name verification pattern
    - monitoring/grafana/provisioning/datasources/prometheus.yml — datasource uid prometheus
  </read_first>
  <behavior>
    - Tests FAIL when model-serving-overview.json absent
    - Parse dashboard JSON; assert exactly 8 panels with correct titles and types
    - Assert uid model-serving-overview, datasource uid prometheus on all targets
    - Assert PromQL strings contain request_count_total, request_duration_bucket, prediction_count_total, up{job="app"}
    - Assert stat panels for p50, p95, Error Rate, Service Health, Total Predictions have graphMode none or equivalent
  </behavior>
  <action>
    Create tests/test_grafana_dashboard.py with pure stdlib json/pathlib (no Docker required).

    test_dashboard_json_exists_and_valid:
    - Load monitoring/grafana/dashboards/model-serving-overview.json
    - Assert dashboard uid == model-serving-overview, title == Model Serving Overview, editable is false

    test_dashboard_has_eight_panels_with_contract:
    - Assert len(panels) == 8 (03-UI-SPEC binding rule)
    - Required titles: Request Rate, p50 Latency, p95 Latency, Error Rate, Prediction Throughput, HTTP Errors by Status, Service Health, Total Predictions
    - Stat type panels: p50 Latency, p95 Latency, Error Rate, Service Health, Total Predictions
    - Timeseries panels: Request Rate, Prediction Throughput, HTTP Errors by Status

    test_dashboard_promql_uses_phase1_metrics:
    - Collect all expr strings from panel targets
    - Assert request_count_total present, request_duration_bucket with path="/predict" present, prediction_count_total present, up{job="app"} present
    - Assert no http_requests_total reference

    test_dashboard_provider_yaml_exists:
    - Load monitoring/grafana/provisioning/dashboards/dashboard.yml
    - Assert provider name model-serving, type file, path /var/lib/grafana/dashboards

    Tests MUST fail RED before Task 2 creates JSON files.
  </action>
  <acceptance_criteria>
    - tests/test_grafana_dashboard.py exists with at least 3 test functions
    - pytest tests/test_grafana_dashboard.py fails RED before dashboard JSON exists
    - Tests reference all 8 panel titles from 03-UI-SPEC.md
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && test -f tests/test_grafana_dashboard.py && grep -q 'model-serving-overview' tests/test_grafana_dashboard.py && (pytest tests/test_grafana_dashboard.py -x 2>&1 | grep -qiE 'fail|error|FileNotFound|no such file' || test $? -ne 0)</automated>
  </verify>
  <done>Dashboard contract tests exist and are RED awaiting dashboard JSON (TDD gate for MON-02).</done>
</task>

<task type="auto">
  <name>Task 2: Model Serving Overview dashboard and home-dashboard wiring</name>
  <files>monitoring/grafana/dashboards/model-serving-overview.json, monitoring/grafana/provisioning/dashboards/dashboard.yml, docker-compose.yml</files>
  <read_first>
    - .planning/phases/03-local-dev-stack-dashboards/03-UI-SPEC.md — full layout, colors, copywriting, panel grid coordinates
    - .planning/phases/03-local-dev-stack-dashboards/03-PATTERNS.md — panel inventory table and bind-mount pairing
    - docker-compose.yml — extend grafana service volumes and environment from Plan 03-01
  </read_first>
  <action>
    Create monitoring/grafana/provisioning/dashboards/dashboard.yml per RESEARCH Pattern 3:
    - apiVersion 1, provider name model-serving, orgId 1, folder empty string, type file, disableDeletion false, updateIntervalSeconds 10, options.path /var/lib/grafana/dashboards

    Create monitoring/grafana/dashboards/model-serving-overview.json per 03-UI-SPEC.md (D-01, D-02, D-03, D-04):
    - Metadata: uid model-serving-overview, title Model Serving Overview, editable false, refresh 10s, timezone browser, tags model-serving/portfolio/phase-3
    - Description: Live metrics for the basic-model-serving API. Generate traffic with curl POST /predict — see README quickstart.
    - Exactly 8 panels with grid positions from UI-SPEC (Request Rate 0,0,12,8; p50 12,0,6,4; p95 18,0,6,4; Error Rate 12,4,12,4; Throughput 0,8,12,8; HTTP Errors 12,8,12,8; Service Health 0,16,6,4; Total Predictions 6,16,6,4)
    - All panel datasource: type prometheus, uid prometheus
    - Stat panels (p50, p95, Error Rate, Service Health, Total Predictions): type stat, graphMode none
    - Latency stat panels: unit ms, multiply PromQL by 1000, thresholds absolute steps 100 yellow 200 red
    - Service Health: value mappings 1→UP green, 0→DOWN red
    - HTTP Errors panel: two queries 4xx blue #5794F2, 5xx red #F2495C
    - Time series: drawStyle line, fillOpacity 10, showPoints never
    - Panel links: [] on all panels (no drill-down per UI-SPEC)
    - Use route template path="/predict" in latency queries — never raw URLs

    Update docker-compose.yml grafana service (D-04):
    - Add volume ./monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    - Add environment GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/var/lib/grafana/dashboards/model-serving-overview.json
    - Keep existing provisioning volume mount

    Do NOT create dashboard via Grafana UI export workflow as primary path — JSON is source of truth in git (D-15, MON-05). Do NOT add template variables. Do NOT use community panel plugins.

    Extend tests/test_compose_stack.py with test_grafana_dashboard_has_data_after_traffic (@pytest.mark.docker):
    - After compose up and /health/ready, POST /predict with tests/fixtures/sample.jpg
    - Authenticate to Grafana API (admin/admin) and query Prometheus datasource via Grafana proxy OR assert home dashboard JSON via Grafana search API returns model-serving-overview
    - Assert at least one panel query returns non-empty result (request_count_total rate &gt; 0 or equivalent)
    - Skip if Docker unavailable (same _docker_available() helper as 03-01)
  </action>
  <acceptance_criteria>
    - pytest tests/test_grafana_dashboard.py passes GREEN
    - docker compose up -d; after curl predict traffic, Grafana home dashboard shows non-empty Request Rate and p50/p95 stat values
    - Login lands on Model Serving Overview not generic Grafana home (D-04)
    - Dashboard Browse lists Model Serving Overview uid model-serving-overview
    - grep -v '^#' monitoring/grafana/dashboards/model-serving-overview.json | grep -c 'request_count_total' returns at least 3
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_grafana_dashboard.py -x && grep -v '^#' monitoring/grafana/dashboards/model-serving-overview.json | grep -q 'model-serving-overview' && grep -v '^#' docker-compose.yml | grep -q 'GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH' && pytest tests/test_compose_stack.py -m docker -k grafana_dashboard_has_data -x</automated>
  </verify>
  <done>MON-02 and MON-05 dashboard portion satisfied. Reviewer sees 8-panel ops-standard dashboard with live data (D-01, D-02, D-03, D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana → Prometheus | Server-side PromQL queries via provisioned proxy datasource |
| Dashboard JSON → Browser | Read-only provisioned dashboard (editable: false) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Dashboard JSON in git | mitigate | editable: false; changes only via repo (D-15) |
| T-03-06 | Information Disclosure | PromQL exposes aggregate metrics only | accept | No PII; route template paths only |
| T-03-07 | Repudiation | Empty panels on wrong metric names | mitigate | Static tests assert request_count_total not http_requests_total |

</threat_model>

<verification>
- pytest tests/test_grafana_dashboard.py passes
- Grafana home dashboard shows Model Serving Overview after admin login
- All 8 panels render with data after curl traffic
</verification>

<success_criteria>
- MON-02: 8 panels (5-7 minimum) with real live metric data
- MON-05: Dashboard provisioned as code via YAML provider + JSON file
- D-01, D-02, D-03, D-04 implemented per UI-SPEC
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `monitoring/grafana/dashboards/model-serving-overview.json` | 03-02 | 8-panel ops-standard dashboard (MON-02, D-01–D-04) |
| `monitoring/grafana/provisioning/dashboards/dashboard.yml` | 03-02 | File-based dashboard provisioning (MON-05) |
| `tests/test_grafana_dashboard.py` | 03-02 | Static dashboard contract validation |
| `docker-compose.yml` (grafana volumes/env) | 03-02 | Home dashboard path + dashboards bind-mount (D-04) |

<output>
Create .planning/phases/03-local-dev-stack-dashboards/03-02-SUMMARY.md when done
</output>
