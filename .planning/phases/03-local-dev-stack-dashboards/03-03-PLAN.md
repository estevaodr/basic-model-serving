---
phase: 03-local-dev-stack-dashboards
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - monitoring/grafana/provisioning/alerting/downtime.yml
  - README.md
  - .env.example
  - tests/test_grafana_alerting.py
autonomous: true
requirements:
  - MON-03
  - MON-05
  - CONT-04
user_setup: []

must_haves:
  truths:
    - "Grafana Alerting shows Service Down rule provisioned on compose startup"
    - "Stopping API container for 60+ seconds transitions alert to Firing in Grafana UI"
    - "Restarting API returns alert to Normal within ~60 seconds"
    - "README documents docker compose up quickstart, ports, curl traffic, and alert demo steps"
    - "Normal docker compose up boot does not fire downtime alert before model is ready"
  artifacts:
    - path: monitoring/grafana/provisioning/alerting/downtime.yml
      provides: Provisioned Grafana unified alerting rule
      contains: "Service Down"
    - path: README.md
      provides: Local observability stack quickstart and reviewer workflow
      contains: "docker compose up"
    - path: tests/test_grafana_alerting.py
      provides: Static alert provisioning validation
      min_lines: 20
  key_links:
    - from: monitoring/grafana/provisioning/alerting/downtime.yml
      to: up{job="app"}
      via: alert query expression
      pattern: 'job="app"'
    - from: README.md
      to: docker compose stop app
      via: alert demo steps D-10
      pattern: "docker compose stop app"
---

## Phase Goal

**As a** developer, **I want to** start the full API + metrics stack with one `docker compose up`, **so that** I can iterate locally with live Grafana dashboards during development.

<objective>
Complete the observability vertical slice: provisioned downtime alert (Grafana unified alerting), reviewer-ready README quickstart with curl traffic and alert demo, and static validation tests.

Purpose: Phase 3 exit criteria — MON-03 alert demo path, full MON-05 provisioning (datasource + dashboard + alert), CONT-04 documented workflow (D-05–D-10, D-12, D-15, D-16).
Output: Alert YAML, README observability section, .env.example Grafana comments, alerting tests.
</objective>

<execution_context>
@$HOME/.cursor/gsd-core/workflows/execute-plan.md
@$HOME/.cursor/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-local-dev-stack-dashboards/03-CONTEXT.md
@.planning/phases/03-local-dev-stack-dashboards/03-UI-SPEC.md
@.planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md
@README.md
@tests/fixtures/sample.jpg
@monitoring/prometheus/prometheus.yml

<interfaces>
Alert contract (03-UI-SPEC.md, D-09, D-11, D-12):
- Rule name: Service Down
- Query: up{job="app"} == 0
- for: 1m
- evaluation interval: 15s (match scrape_interval)
- datasourceUid: prometheus
- No contact points (UI-only)
- Summary: Model serving API is unreachable
- Description: Prometheus cannot scrape app:8000/metrics for 1 minute. Check docker compose app service.

README alert demo (D-10):
1. docker compose stop app
2. Wait ~60–90s
3. Grafana → Alerting → Service Down → Firing
4. docker compose start app
5. Wait ~60s → Normal
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alert provisioning and static validation tests</name>
  <files>monitoring/grafana/provisioning/alerting/downtime.yml, tests/test_grafana_alerting.py</files>
  <read_first>
    - .planning/phases/03-local-dev-stack-dashboards/03-UI-SPEC.md — Alert Visualization section
    - .planning/phases/03-local-dev-stack-dashboards/03-RESEARCH.md — Pattern 4 downtime alert, UI export recommendation
    - monitoring/prometheus/prometheus.yml — job_name app for up{job="app"} consistency
  </read_first>
  <behavior>
    - test_alert_provisioning_yaml_exists FAILS before downtime.yml created
    - Parse YAML; assert rule title Service Down, query references up and job="app", for duration 1m, datasourceUid prometheus
    - Assert no contact_points or receivers configured (D-12)
  </behavior>
  <action>
    Create tests/test_grafana_alerting.py using stdlib only where possible; use PyYAML only if already in dev deps — otherwise parse with string assertions for required substrings to avoid new dependencies.

    test_downtime_alert_yaml_contract:
    - File monitoring/grafana/provisioning/alerting/downtime.yml must exist
    - Content includes Service Down, up{job="app"}, datasourceUid: prometheus (or datasource_uid), 1m for duration
    - Content must NOT include slack, email, webhook, or contact_point receiver URLs

    Create monitoring/grafana/provisioning/alerting/downtime.yml per D-09/D-11/D-12 and 03-UI-SPEC:
    - Grafana unified alerting file provisioning format (apiVersion 1)
    - Rule group with evaluation interval 15s
    - Alert title Service Down
    - Query: up{job="app"} == 0 against datasource uid prometheus
    - for: 1m
    - Labels: severity=critical, team=model-serving (optional per UI-SPEC)
    - Annotations summary and description per UI-SPEC copywriting contract
    - No contactPoints, muteTimes, or notification policies with receivers

    Implementation approach per RESEARCH: if hand-authoring the data query block is error-prone, spin up stack once, create rule in Grafana UI, export YAML, sanitize and commit — but final artifact must be in git as provisioning file (MON-05).

    Do NOT use Prometheus alerting rules or Alertmanager (D-09). Do NOT add Slack/email contact points (D-12).
  </action>
  <acceptance_criteria>
    - monitoring/grafana/provisioning/alerting/downtime.yml exists
    - pytest tests/test_grafana_alerting.py passes GREEN
    - grep -v '^#' monitoring/grafana/provisioning/alerting/downtime.yml | grep -c 'Service Down' returns at least 1
    - grep -v '^#' monitoring/grafana/provisioning/alerting/downtime.yml | grep -c 'job=.app' returns at least 1
    - File contains no slack webhook or email receiver configuration
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && pytest tests/test_grafana_alerting.py -x && test -f monitoring/grafana/provisioning/alerting/downtime.yml && grep -v '^#' monitoring/grafana/provisioning/alerting/downtime.yml | grep -q 'Service Down' && grep -v '^#' monitoring/grafana/provisioning/alerting/downtime.yml | grep -q 'app'</automated>
  </verify>
  <done>MON-03 and MON-05 alerting portion satisfied at config level (D-09, D-11, D-12). Service Down rule provisioned as code.</done>
</task>

<task type="auto">
  <name>Task 2: README observability quickstart and reviewer workflow</name>
  <files>README.md, .env.example</files>
  <read_first>
    - README.md — existing Docker section to preserve (D-16)
    - .planning/phases/03-local-dev-stack-dashboards/03-PATTERNS.md — README additions list
    - tests/test_docker_config.py — .env.example must keep only Settings fields in uncommented lines
    - tests/fixtures/sample.jpg — curl example image path
  </read_first>
  <action>
    Add new README section ## Local observability stack after existing ## Docker section (do NOT remove uv run docker-build|docker-run|docker-smoke content per D-16):

    1. Prerequisites callout: run uv run docker-build before first docker compose up (D-14)
    2. Hero command: docker compose up (D-05) — not uv wrapper
    3. Port table (D-07): API :8000, Grafana :3000, Prometheus :9090
    4. Grafana login admin/admin (D-08); note lands on Model Serving Overview home dashboard (D-04)
    5. Manual curl traffic (D-06) — at least two examples:
       - File upload: curl -s -X POST http://localhost:8000/predict -F "file=@tests/fixtures/sample.jpg"
       - Optional 4xx: curl -s -X POST http://localhost:8000/predict -F "file=@README.md"
    6. Image-only iteration (D-13): uv run docker-build then docker compose restart app
    7. Alert demo steps (D-10): docker compose stop app → wait ~60-90s → Grafana Alerting Service Down Firing → docker compose start app → Resolved
    8. Config reload (D-15): edit monitoring/ files → docker compose restart grafana or prometheus
    9. Troubleshooting: if panels empty, verify Prometheus target app UP at :9090/targets

    Update .env.example per D-08 without breaking test_env_example_matches_settings_fields:
    - Keep four uncommented app vars unchanged (TORCH_NUM_THREADS, MAX_UPLOAD_BYTES, URL_TIMEOUT, LOG_LEVEL)
    - Add commented section below documenting Grafana compose-only vars:
      # Grafana (compose stack only — not read by FastAPI app)
      # GF_SECURITY_ADMIN_USER=admin
      # GF_SECURITY_ADMIN_PASSWORD=admin
    - Do NOT add uncommented GF_* keys (would fail test_docker_config.py per 03-PATTERNS.md)

    Preserve entire existing Docker section verbatim structure.
  </action>
  <acceptance_criteria>
    - README contains docker compose up as primary stack command
    - README contains port table with 8000, 3000, 9090
    - README contains curl example with tests/fixtures/sample.jpg
    - README contains alert demo with docker compose stop app and start app
    - README mentions uv run docker-build prerequisite
    - pytest tests/test_docker_config.py still passes (Settings field contract preserved)
    - .env.example commented Grafana section documents admin credentials
  </acceptance_criteria>
  <verify>
    <automated>cd /home/estevao/src/brenz/basic-model-serving && grep -q 'docker compose up' README.md && grep -q '3000' README.md && grep -q 'tests/fixtures/sample.jpg' README.md && grep -q 'docker compose stop app' README.md && pytest tests/test_docker_config.py -x</automated>
  </verify>
  <done>CONT-04 workflow fully documented. Reviewer can clone → docker-build → compose up → curl → see dashboards → demo alert (D-05–D-10, D-13–D-16).</done>
</task>

<task type="checkpoint:human">
  <name>Task 3: Alert Firing/Resolved demo verification</name>
  <files>README.md</files>
  <read_first>
    - README.md — alert demo steps written in Task 2
    - .planning/phases/03-local-dev-stack-dashboards/03-CONTEXT.md — D-10, D-11, D-12
  </read_first>
  <action>
    With full stack running (`docker compose up`), perform the README alert demo manually:
    1. Confirm Service Down rule appears in Grafana → Alerting before stopping API
    2. Run `docker compose stop app` and wait 60–90 seconds
    3. Confirm alert state transitions to **Firing** in Grafana Alerting UI (D-10, D-11)
    4. Run `docker compose start app`, wait for /health/ready, confirm alert returns to **Normal** within ~60s
    5. Confirm normal compose boot (fresh `docker compose up`) does NOT fire alert during model load window

    Record pass/fail in plan SUMMARY or note any timing adjustments needed for D-11 1m threshold.
  </action>
  <acceptance_criteria>
    - Alert transitions Firing after sustained API stop (≥60s)
    - Alert returns Normal after API restart
    - No false Firing during healthy startup
  </acceptance_criteria>
  <verify>
    <human>Operator confirms Grafana Alerting UI shows Firing then Normal per steps above</human>
  </verify>
  <done>MON-03 runtime behavior verified at human checkpoint (must_have truth gated at execution).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana Alerting UI | Local-only alert state visibility; no outbound notifications |
| README instructions | Developer executes docker compose stop/start for demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Denial of Service | Alert noise on compose boot | mitigate | Inherited from 03-01 health gating; alert for 1m only after sustained scrape failure (D-11) |
| T-03-09 | Information Disclosure | README exposes default Grafana creds | accept | Local portfolio demo only; explicit D-08 decision |
| T-03-10 | Spoofing | No webhook contact points | accept | UI-only alerting reduces external attack surface (D-12) |

</threat_model>

<verification>
- Grafana Alerting page lists Service Down rule after compose up
- docker compose stop app → alert Firing after ~60-90s (checkpoint:human Task 3)
- README quickstart complete; test_docker_config.py passes
- Full phase test suite: pytest tests/test_compose_stack.py tests/test_grafana_dashboard.py tests/test_grafana_alerting.py
</verification>

<success_criteria>
- MON-03: Downtime alert configured and demo steps documented
- MON-05: Complete provisioning-as-code (datasource + dashboard + alert)
- CONT-04: Documented single-command local dev stack workflow
- D-05, D-06, D-08, D-10, D-12, D-15, D-16 implemented
</success_criteria>

## Artifacts this phase produces

| Artifact | Plan | Purpose |
|----------|------|---------|
| `monitoring/grafana/provisioning/alerting/downtime.yml` | 03-03 | Service Down unified alert (MON-03, D-09) |
| `tests/test_grafana_alerting.py` | 03-03 | Alert provisioning contract tests |
| `README.md` (Local observability stack) | 03-03 | Quickstart, ports, curl, alert demo (D-05–D-10) |
| `.env.example` (commented Grafana section) | 03-03 | Documents admin creds without breaking Settings test (D-08) |

<output>
Create .planning/phases/03-local-dev-stack-dashboards/03-03-SUMMARY.md when done
</output>
